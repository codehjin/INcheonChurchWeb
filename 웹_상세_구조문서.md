# 인천중앙교회 교육국 회계 시스템 — 현행 구조 상세 문서

> **이 문서의 성격**: 리팩토링 **계획을 세우기 위한 입력 자료**입니다.
> "이렇게 바꾸자"는 방안은 여기 넣지 않고 다음 단계 문서로 미룹니다. 여기에는 **지금 어떻게 생겼는지**만 적습니다.
> 모든 서술에는 `파일:줄` 근거를 붙였습니다.
>
> **기준 시점**: 2026-09-05 / 마지막 커밋 `1d9ebc5` + 미커밋 작업분(모바일 화면 10개, `mobile-app.css/js`)

---

## 0. 다른 문서와의 분담

| 문서 | 줄수 | 다루는 것 | 이 문서와의 관계 |
|---|---|---|---|
| `리팩토링_진행보고서.md` | 578 | 2026-06~08 리팩토링 **이력**과 교훈 | **2026-09 모바일 화면 10개(약 4,600줄)가 기록되지 않음.** 이 문서가 그 공백을 채운다 |
| `생명주기_설계메모.md` | 156 | 제품 철학("기록의 인수인계"), 설계 결정, **작업 안전 수칙** | 함정 목록은 여기서 반복하지 않고 §10에서 링크만 |
| `예산_분류체계_개선안.md` | 118 | 예산 2축 분리 원칙과 유년부 적용 결과 | 2축 원칙은 §3에 요약만, 상세는 원문 참조 |
| `모바일앱_디자인_브리프.md` | 621 | 모바일 앱 16화면 **설계 명세** | 현재 `Mobile*.razor`의 원본 스펙. 구현 현황은 §4 |

---

## 1. 시스템 한눈에

- **ASP.NET Core 8 Blazor Server** — Razor Components, `InteractiveServer` 렌더 모드(SignalR circuit). MVC 컨트롤러·REST API 없음
- **SQLite + EF Core 8** (`church.db`), 코드 우선 마이그레이션 13건
- **Bootstrap 5** + Bootstrap Icons 1.11.3(CDN) + 자체 디자인 시스템(`wwwroot/app.css` "The Tactile Ledger")
- 대화상자는 **SweetAlert2로 `window.alert`/`confirm`을 전역 오버라이드**(`Components/App.razor`)
- OCR은 **Google Cloud Vision V1**(`Services/OcrService.cs`), 월 사용량은 `OcrUsage` 테이블로 카운트

### 규모 — 무게중심이 서비스가 아니라 화면에 있다

| 구분 | 줄수 |
|---|---|
| `Components/**/*.razor` (25개 페이지 + 레이아웃·공용) | **16,470** |
| `Services/` + `Models/` + `Data/` 수기 `.cs` (마이그레이션 제외) | **3,945** |
| 그중 `Services/AccountingService.cs` 단일 파일 | **2,239** (수기 C#의 57%) |
| `Program.cs` | 111 |

razor가 C#의 **4.2배**입니다. 그리고 razor 상위 12개 파일이 12,300줄로 전체의 약 75%를 차지합니다.

### 라우트 22개

| 웹 화면 | 모바일 짝 | 비고 |
|---|---|---|
| `/` 대시보드 | `/dashboard` | 자동 교체 대상 |
| `/monthly` 월별 장부 | `/ledger` (+ `/ledger/print`) | 자동 교체 대상 |
| `/annual-plan` 연간 계획 | `/plan` | 자동 교체 대상 |
| `/weekly-meeting` 주간 회의록 | `/plan` (세그먼트) | 자동 교체 대상 |
| `/expense-report-quarterly` 지출결의서 | `/resolution` | 자동 교체 대상 |
| `/event-report` 행사 보고서 (+ `/event-report/edit`) | `/event-report-app` | 자동 교체 대상 |
| `/settings` 환경설정 | `/settings-app` | 자동 교체 대상 |
| `/data-collection` 수집 및 정리 | `/receipt`, `/matching` | 짝이 완전하지 않음 |
| `/manual` 도움말 | — | 웹 전용 |
| `/login`, `/Error` | — | |
| — | `/more` 더보기 | 모바일 전용 |

---

## 2. 부트스트랩과 구성 — `Program.cs` (111줄)

**기동 순서**

1. **Docker 경로 하드코딩** (L11-32) — `Directory.Exists("/app/wwwroot")`이면 `SetCurrentDirectory("/app")`,
   `ContentRootPath="/app"`, `WebRootPath="wwwroot"`. 주석에 CS8852 회피용이라 명시
2. **Vision 키 탐색** (L35-47) — ContentRoot에서 `google-vision-key.json`을 찾아
   `GOOGLE_APPLICATION_CREDENTIALS` 환경변수 설정. **없으면 `FileNotFoundException`으로 앱이 아예 뜨지 않는다**
3. **DataProtection** (L50-56) — `./keys` 디렉터리에 키 파일 영속화, `SetApplicationName("INcheonChurchWeb")`

**DI 등록 전체**

| 등록 | 수명 |
|---|---|
| `AddDataProtection().PersistKeysToFileSystem(keys)` | — |
| `AddRazorComponents().AddInteractiveServerComponents()` (`MaximumReceiveMessageSize = 10MB`) | — |
| `AddDbContextFactory<AppDbContext>(UseSqlite(...))` | 팩토리 |
| `AccountingService` | Scoped |
| `FileService` | Scoped |
| `OcrService` | Scoped |
| `TwoFactorAuthService` | Scoped |
| `AutoBackupService` | **HostedService (유일한 백그라운드 서비스)** |

`PasswordHasher`는 DI에 없고 static 유틸로 직접 호출합니다.
연결 문자열은 `GetConnectionString("DefaultConnection") ?? "Data Source=church.db"`.

**미들웨어 파이프라인** (L92-104)

```
(!IsDevelopment) → UseExceptionHandler("/Error") + UseHsts()
UseHttpsRedirection → UseStaticFiles → UseAntiforgery
MapRazorComponents<App>().AddInteractiveServerRenderMode()
```

⚠️ **인증·인가 미들웨어가 없습니다.** 로그인은 `Components/Layout/MainLayout.razor`가
`ProtectedLocalStorage`에 **username 문자열만** 저장하고(직렬화 깨짐 회피), 첫 렌더에서 DB로 `User`를
`Department`까지 포함해 재조회한 뒤 `CascadingValue`로 내려주는 방식입니다.
**라우트 보호는 전적으로 페이지별 `CurrentUser` null 체크와 역할 프로퍼티 분기에 의존합니다.**
URL을 직접 치면 컴포넌트가 렌더된 뒤 자체 판단으로 막습니다.

**DB 초기화** (L106-111) — `app.Run()` 직전에 스코프를 만들어 `DbInitializer.Initialize(db)` 호출.
`Data/DbInitializer.cs:18`이 `Database.Migrate()` → 부서 시드 → 기본 유저 시드 → 2026 예산 시드 →
인코딩 손상 데이터 정화 순으로 수행합니다.

⚠️ `Data/DbInitializer.cs:15-17` — 레거시 `church.db`는 `__EFMigrationsHistory`가 없어
**앱 기동 전 기존 마이그레이션 베이스라인 등록을 1회 해야 하며, 안 하면 "table already exists"로 기동 실패**합니다.

---

## 3. 도메인 모델과 불변 규칙

리팩토링에서 **절대 깨지면 안 되는 규칙**들입니다. 나중에 테스트로 굳힐 대상이 여기 있습니다.

### 3.1 엔티티 (`Data/AppDbContext.cs`, DbSet 18개)

**레거시 세트** — `Department`, `User`, `LedgerEntry`(DbSet 이름은 `Transactions`), `BudgetPlan`,
`CategoryMapping`, `QuarterClose`, `ActivityLog`, `DataBackup`, `ExpenseReport`, `DepartmentOfficer`,
`UploadedReceipt`, `OcrUsage`

**생명주기 세트** (`BaseEntity` 상속 — `CreatedAt/By`, `UpdatedAt/By`, `IsDeleted` 소프트 삭제 + 글로벌 쿼리 필터) —
`AnnualPlan`, `WeeklyMeeting`, `ExpenseResolution`, `Receipt`, `LedgerTransaction`, `EventReport`

⚠️ **이름이 비슷한 서로 다른 엔티티가 있습니다**: `ExpenseReport`(지출결의서, 레거시) ≠ `EventReport`(행사 사후보고서),
`LedgerEntry`(현재 쓰는 장부) ≠ `LedgerTransaction`(생명주기 세트, 이관 대상).

### 3.2 회계 달력 — 가장 위험한 규칙

- 회계연도는 1~12월이 아니라 **11월 넷째 주 일요일에 끝난다**
- 분기: **Q1 = 12~2월, Q2 = 3~5월, Q3 = 6~8월, Q4 = 9~11월**
- **부서별로 분기 경계를 재정의할 수 있다**
- 부서별 설정이 전용 엔티티가 아니라 **`CategoryMappings` 테이블에 `Quarter_{year}_Q{n}` 키워드 /
  `"시작~끝"` 문자열**로 저장된다 (`Services/AccountingService.cs:134-152`).
  전용 테이블 없이 문자열 키/값으로 우회한 설계이며, 파싱 실패 시 조용히 기본 규칙으로 떨어진다
- 11·12월은 앞선 달력 연도에 속한다 — `calYear = (month == 11 || month == 12) ? year - 1 : year`
  (`AccountingService.cs:1893` 부근, `MonthlyLedger.razor:1258` 부근에 같은 식이 복제되어 있음)

### 3.3 2축 원칙

- **지출**: `Category` = 행사(WHY) / `SubCategory` = 비용 종류(WHAT)
- **수입**: `Category` = `회비수입`·`찬조금`·`교회보조금`·`주일헌금` / `SubCategory` = 행사명
- 상세와 적용 결과는 `예산_분류체계_개선안.md` 참조

### 3.4 "행사 = 예산 카테고리" 단일 키

`여름성경학교`라는 **문자열 하나**가 `BudgetPlan.Category` ↔ `Transactions.Category` ↔
`AnnualPlan.Title` ↔ `EventReport.EventName`을 잇습니다. 외래키가 아니라 문자열 일치이므로,
공백·오타 하나로 집계가 끊깁니다.

### 3.5 이월금 계산

```
이월금 = 예산의 Category.Contains("이월") 항목 금액
       + (회계연도 시작 이전 모든 거래의 수입 − 지출)
       + (조회 기간 시작 이전, 회계연도 안의 수입 − 지출)
```

이 식이 **두 곳에 복제**되어 있습니다 (§6.2).

### 3.6 역할 (`Models/AppModel.cs:51-55`)

| 역할 | 화면 표기 | 프로퍼티 |
|---|---|---|
| `Admin` | 최고관리자 | 전부 true |
| `Manager` | 부서운영자 | `CanEdit`, `CanEditLedger`, `CanAccessDataCollection`, `CanAccessExcelTab` |
| `Auditor` | 감사·국장 | `CanViewAll`만 (읽기 전용, 전 부서) |
| `User` | 일반조회(교사) | `CanAccessDataCollection`만 |

⚠️ `CanEdit`와 `CanEditLedger`는 **정의가 완전히 동일**합니다(`Role == "Manager" || Role == "Admin"`).
이름이 둘로 갈려 있어 호출부마다 다른 걸 쓰고 있고, 지금은 결과가 같지만 한쪽만 바꾸면 조용히 갈라집니다.

---

## 4. 화면 지도

### 4.1 웹 화면

| 화면 | 라우트 | 줄수 | markup : @code | 역할 | 비고 |
|---|---|---:|---|---|---|
| 환경설정 | `/settings` | 2091 | 1244 : **847** | 역할별 탭 차등 | 최대 파일 |
| 월별 장부 | `/monthly` | 1625 | 1078 : **547** | 조회 전체 / 편집 Manager+ | markup 중 **400줄이 `<style>`**(인쇄 CSS) |
| 수집 및 정리 | `/data-collection` | 1218 | **156 : 1062** | `CanAccessDataCollection` | **markup보다 코드가 7배 — 사실상 C# 파일** |
| 지출결의서 | `/expense-report-quarterly` | 1150 | 852 : 298 | Manager+ | markup 대부분이 인쇄 규격(135×195mm) |
| 연간 계획 | `/annual-plan` | 1147 | 545 : **602** | Manager+ | |
| 대시보드 | `/` | 808 | 578 : 230 | 전체 | markup 중 252줄이 `<style>` |
| 도움말 | `/manual` | 657 | 542 : 115 | Manager+ | 정적 문서 — 정상 |
| 주간 회의록 | `/weekly-meeting` | 569 | 356 : 213 | Manager+ | |
| 행사 보고서 작성 | `/event-report/edit` | 332 | — | Manager+ | |
| 로그인 | `/login` | 252 | — | — | `MainLayout`이 인라인 렌더도 함 |
| 행사 보고서 | `/event-report` | 247 | — | Manager+ | |

`/data-collection` 안에 자식 컴포넌트 4개: `ReceiptSingleUpload`(166), `ReceiptMultiUpload`(82),
`BankImport`(328), `LedgerMatching`(222).

### 4.2 모바일 화면 (2026-09 신규, 기존 보고서에 없음)

| 화면 | 라우트 | 줄수 | markup : @code | 비고 |
|---|---|---:|---|---|
| 월별 장부 + 필터/편집 시트 + 인쇄 시트 | `/ledger` | 998 | 657 : 341 | markup 중 **239줄이 `<style>`** |
| 영수증 촬영·OCR·일괄 (3단계 한 컴포넌트) | `/receipt` | 888 | 465 : **423** | `_step` 1·2·3 |
| 계획 + 회의록 (세그먼트 + 시트 2종) | `/plan` | 650 | 354 : 296 | |
| 홈 대시보드 | `/dashboard` | 513 | 421 : 92 | markup 중 176줄이 `<style>`. **@code 92줄 — 가장 얇은 뷰** |
| 환경설정 (5개 하위 섹션) | `/settings-app` | 476 | 318 : 158 | |
| 지출결의서 | `/resolution` | 351 | — | |
| 장부 인쇄 전용 | `/ledger/print` | 324 | — | 레이아웃 껍데기를 CSS로 숨김 |
| 행사 보고서 | `/event-report-app` | 317 | — | |
| 장부 매칭 | `/matching` | 219 | — | |
| 더보기 | `/more` | 150 | — | 웹 전용 기능으로 가는 출구 |

### 4.3 웹 ↔ 모바일 자동 교체 (`Components/Layout/MainLayout.razor`)

- 판정: **폭 ≤ 640px** 이거나 **(`pointer: coarse` && 폭 ≤ 1024px)** — 두 번째 조건은 Galaxy Z Fold 펼친 화면(약 690~900px)용
- 같은 규칙을 **CSS와 JS 양쪽에 중복 정의**: `wwwroot/mobile-app.css`의 미디어 쿼리 3곳 +
  `wwwroot/mobile-app.js`의 `maViewport.isMobile()`. 한쪽만 바꾸면 라우팅과 레이아웃이 어긋난다
- `WebToApp` / `AppToWeb` 딕셔너리로 짝을 정의하고, 첫 렌더·로그인 직후·경로 변경·리사이즈 때 `SyncRouteAsync()`가 이동
- `?web=1`이 붙으면 모바일에서도 웹 화면을 그대로 본다 (더보기 → "웹 화면으로 열기")
- 앱 화면에서는 `body.ma-app-route` 클래스로 레이아웃의 모바일 상단 헤더를 숨긴다

### 4.4 레이아웃 (`MainLayout.razor` 397줄, markup 248 : code 149)

세션 복원·로그인·로그아웃·화면 교체 라우팅이 **모두 레이아웃 컴포넌트 안에** 있습니다.
그 외에 데스크톱에서 모든 요소의 최소 폰트를 20px로 강제하는 인라인 스크립트가 있고,
앱 디자인이 깨지지 않도록 `data-no-font-bump` 속성을 가진 조상 아래는 건너뜁니다.

---

## 5. 서비스 계층 — `AccountingService.cs` 해부

**2,239줄 / `public` 멤버 115개(그중 `public async Task` 78개) / DTO 클래스 13개가 파일 안에 인라인.**
생성자는 `IDbContextFactory<AppDbContext>` + `IWebHostEnvironment` (L89).
파일 상단 L1-77에는 DTO 6종이 **클래스 밖 네임스페이스 레벨**에 흩어져 있습니다.

### 5.1 섹션 맵

| 배너 | 줄범위 | 주요 메서드 |
|---|---|---|
| `[1-1] 보조금 현황 계산` (실제로는 분기 날짜 계산) | 94-153 | `GetFourthSundayOfMonth` `GetFourthSundayOfNovember` `GetDefaultQuarterRange` `GetQuarterDateRangeAsync` |
| `[1-2] 분기 설정 저장/조회` | 154-181 | `SaveQuarterSettingAsync` |
| `[1-3] 월별장부 기간별 조회` | 182-228 | `GetLedgerByOptionAsync` `GetAllCategoriesAsync` `BulkUpdateCategoryAsync` `GetAllBudgetPlansForDeptAsync` |
| `1. 대시보드 통계` | 229-268 | `GetIntegratedDashboardAsync` `GetDashboardDataAsync` |
| `2. 영수증 이미지 최적화 업로드` | 269-402 | `UploadReceiptAsync`(ImageSharp 리사이즈 L316-318) `RemoveReceiptAsync` |
| `3. 로그 및 백업` | 403-507 | `LogActivityAsync` `CreateBackupAsync` `GetBackupsAsync` `RestoreFromBackupAsync` |
| `4. 장부 관리 및 자동 분류` | 508-590 | `GetCategorySuggestionsAsync` `AddTransactionAsync` `AddTransactionsAsync` `UpdateTransactionAsync` `DeleteTransactionAsync` `GetMonthlyTransactionsAsync` `GetAllTransactionsAsync` |
| `[1회성] 레거시 → LedgerTransaction` | 591-731 | `MigrateLedgerDataAsync` |
| CSV/XLS 은행 거래내역 파싱 | 732-908 | `ParseAndClassifyBankCsvAsync` `GetQuarterNumberAsync` + private `SplitCsvLine` `ClassifyTransaction` |
| `5. 예산 및 분류 설정` | 909-954 | `GetBudgetPlansAsync` `SaveBudgetPlanAsync` `DeleteBudgetPlanAsync` `GetMappingsAsync` `SaveMappingAsync` `DeleteMappingAsync` |
| 환경설정 엑셀 백업/복원 | 955-1100 | `ExportSettingsExcelAsync` `ImportSettingsExcelAsync` |
| `[추천 시스템]` 자동분류 규칙 분석 | 1101-1203 | `AnalyzeMappingSuggestionsAsync` + `Tokenize` |
| `6. 사용자·부서 관리` | 1204-1296 | `GetAllUsersAsync` `AddUserAsync` `DeleteUserAsync` `ChangePasswordAsync` `ResetPasswordAsync` `GetDepartmentsAsync` `GetDepartmentAsync` `UpdateDepartmentAsync` `UpdateUserAsync` `EnsureDetailedMappingsAsync` |
| `[7] 지출결의서` | 1297-1319 | `SaveExpenseReportAsync` `GetExpenseReportsAsync` `DeleteExpenseReportAsync` |
| 지출결의서용 예산/신청액 통계 | 1320-1440 | `GetSubsidyStatusForReportAsync` `GetLedgerAsync` `DeleteLedgerEntryAsync` `CalculateFiscalYearAsync` `BulkRecalcFiscalYearQuarterAsync` |
| **(배너 끊김)** 연간 계획 날짜 계산 | 1441-1579 | `CalcPlanDateInfoAsync` `GetWeekOfYear` `GetWeekOfMonth` `GetMatchingDataAsync` `MatchReceiptAsync` `AutoMatchReceiptsAsync` |
| `📊 홈 대시보드 (모바일 화면용)` | 1580-1723 | DTO `CategoryStat`/`SubsidyRequestItem`/`DashboardView` + `GetDashboardAsync` + private `BuildCategoryStats` |
| (무배너) 분류 자동완성 | 1724-1849 | DTO `CategoryMaps` + `GetCategoryMapsAsync` `CreateLedgerEntryAsync` `SaveLedgerEntryEditAsync` |
| (무배너) 장부 뷰 | 1850-1940 | DTO `LedgerView` + `GetLedgerViewAsync` |
| (무배너) 연간계획 | 1941-2038 | `GetAnnualPlansAsync` `GetAllAnnualPlansAsync` `GetAnnualPlansAllYearsAsync` `GetAllAnnualPlansAllYearsAsync` `SaveAnnualPlanAsync` `AddAnnualPlansBulkAsync` |
| (무배너) 행사 결산·사후보고 | 2039-2174 | DTO `EventFinance` + `GetEventFinancesAsync` `GetEventReportsAsync` `GetEventReportAsync` `GetEventReportByNameAsync` `SaveEventReportAsync` `DeleteEventReportAsync` `DeleteAnnualPlanAsync` |
| (무배너) 주간 회의록 | 2175-2239 | `GetWeeklyMeetingsAsync` `GetWeeklyMeetingsAllYearsAsync` `SaveWeeklyMeetingAsync` `DeleteWeeklyMeetingAsync` |

**배너 구획이 L1441에서 완전히 끊깁니다.** 그 이후 약 800줄(모바일 대응분)에는 구획이 없고,
`생명주기_설계메모.md`의 "대형 파일은 덮어쓰지 말고 블록만 추가" 수칙 때문에 계속 뒤에 쌓이는 구조입니다.

### 5.2 서비스 내부 중복

- **분기·회계연도 계산 6갈래**: `GetDefaultQuarterRange`(115, 순수 계산) / `GetQuarterDateRangeAsync`(134, DB 오버라이드) /
  `GetQuarterNumberAsync`(867, 위를 3번 호출) / `CalculateFiscalYearAsync`(1363) /
  `BulkRecalcFiscalYearQuarterAsync`(1385) / `CalcPlanDateInfoAsync`(1452). 여기에 `Models/DateHelper.cs`가 또 있음(§6.1)
- **대시보드 3중**: `GetIntegratedDashboardAsync`(232) / `GetDashboardDataAsync`(257) / `GetDashboardAsync`(1622).
  **앞의 둘은 razor 어디서도 호출되지 않는 죽은 코드**
- **장부 조회 6종**: `GetLedgerByOptionAsync`(185) `GetMonthlyTransactionsAsync`(580) `GetAllTransactionsAsync`(585)
  `GetLedgerAsync`(1347) `GetLedgerViewAsync`(1865) `GetMatchingDataAsync`(1486) — 필터 축만 다르고 쿼리 골격은 같음
- **연간계획 조회 4종**(1941/1952/1963/1974) — `deptId 유무 × fiscalYear 유무` 조합을 메서드로 전개
- **타입 판별 3세트**: `IsExpenseType`/`IsIncomeType`(1615/1618) vs `IsExpenseTypeString`/`IsIncomeTypeString`(691/697) vs
  인라인 문자열 비교 `b.Type == "Income" || b.Type == "수입"`가 곳곳에 산재

### 5.3 나머지 서비스

| 파일 | 줄수 | 역할 |
|---|---|---|
| `Services/OcrService.cs` | 301 | Vision API 호출 + 날짜/금액 파싱. **키워드 배열 순서가 곧 우선순위**(L25-41) |
| `Services/AutoBackupService.cs` | 95 | 유일한 HostedService |
| `Services/FileService.cs` | 88 | 경로·파일명 헬퍼 + 저장 2종. **저장 흐름 자체는 호출부가 각자 조립** |
| `Services/PasswordHasher.cs` | 82 | PBKDF2. ⚠️ `Verify()`가 **평문도 함께 처리**(L14) — 평문 잔여 계정 폴백이 살아있음 |
| `Services/TwoFactorAuthService.cs` | 42 | TOTP |
| `Models/BoardSearch.cs` | 149 | 연간계획·회의록 검색어 파서 (순수 함수 — 테스트하기 가장 쉬운 코드) |
| `Models/DateHelper.cs` | 37 | 회계연도·분기 **동기·부서 무관** 계산 (§6.1) |

---

## 6. 중복 지도

리팩토링 우선순위를 정하는 근거입니다. 6.2~6.4는 **서비스에 이미 같은 구현이 있어, 새 코드 없이
razor 쪽을 지우고 서비스 호출로 바꾸기만 하면 되는** 즉시 회수 가능한 부채입니다.

### 6.1 회계연도/분기 이중 구현 — 규칙까지 다름 ⚠️ 데이터 정합성 리스크

| | `Models/DateHelper.cs` | `Services/AccountingService.cs` |
|---|---|---|
| 방식 | 동기, **부서 무관** | 비동기, **부서별 DB 설정 우선** |
| 분기 판정 | **월 번호 하드코딩** (`DateHelper.cs:19-27`) | `CategoryMappings`의 `Quarter_{year}_Q{n}` 조회 후 없으면 기본 규칙 (`:134-152`) |
| 회계연도 | `CalculateFiscalYear`(`:5-17`) | `CalculateFiscalYearAsync`(`:1363`) |

`DateHelper`를 쓰는 곳은 두 파일뿐입니다.

- `Components/Pages/DataCollectionHub.razor:743,756,851,858` — **은행 CSV·장부 임포트 시 `FiscalYear`/`Quarter`를 저장**
- `Components/Pages/WeeklyMeetingBoard.razor:450-451` — 검색 필터

즉 **임포트 경로는 달력 기준으로 저장하고, 화면 조회는 부서별 DB 설정으로 필터링**합니다.
부서가 분기 경계를 재정의했다면 경계일 근처 데이터가 저장값과 조회 조건 사이에서 어긋납니다.

### 6.2 장부 로딩 — 거의 축어적 복제

- `Components/Pages/MonthlyLedger.razor:1189-1279` `LoadData()`
- `Services/AccountingService.cs:1865-1940` `GetLedgerViewAsync()`

같은 알고리즘입니다: q1/q4로 회계연도 범위 → `Transactions` 조회 → 예산의 `Contains("이월")` →
회계연도 이전 합산 → 11·12월 달력연도 보정 → 월/분기 스코프 → running balance 누적.
변수명만 다릅니다(`ms`/`me` vs `beforeRange`/`prevDataIn_currentYear`).

`MobileLedger.razor`와 `MobileLedgerPrint.razor`는 서비스 버전을 쓰고, **PC의 `MonthlyLedger`만 자기 복제본**을 씁니다.

### 6.3 대시보드 집계 — DTO·헬퍼까지 이중 정의

| | `Components/Pages/Home.razor` | `Services/AccountingService.cs` |
|---|---|---|
| 집계 | `:691-778` `LoadDashboardData()` | `:1622-1694` `GetDashboardAsync()` |
| 항목별 통계 | `:782-808` `BuildCategoryStats()` (예산 0 처리 `:805`) | `:1697-1722` `BuildCategoryStats()` (`:1716`) |
| DTO | `:594-602` `CategoryStat`, `:633-640` `SubsidyRequestItem` | `:1584-1596` 같은 이름 |
| 타입 판별 | `:688-689` | `:1615-1619` |

⚠️ **계산이 한 줄 다릅니다** — 예산이 0일 때 `Home`은 `실적 > 0 ? 100 : 0`, 서비스는 `0`.
같은 데이터로 웹과 모바일이 다른 퍼센트를 보여줄 수 있습니다.

### 6.4 분류맵 구축 — 판별 기준 불일치

- `Components/Pages/MonthlyLedger.razor:1210-1243` (인라인 조립 + **하드코딩 기본값** `{주일헌금, 십일조…}`, `{예배, 교육비…}`)
- `Services/AccountingService.cs:1740-1789` `GetCategoryMapsAsync()` (`CategoryMaps` DTO, 하드코딩 기본값 없음)

⚠️ 수입/지출 판별이 다릅니다 — `MonthlyLedger.razor:1236`은 `t.Income > 0`, `AccountingService.cs:1775`는 `t.Type == "수입"`.

### 6.5 영수증 업로드/OCR — 3경로, 정책이 각각 다름

| 경로 | 위치 | 이미지 리사이즈 | OCR 사용량 갱신 |
|---|---|---|---|
| 웹 단일 | `DataCollectionHub.razor:432-505` | ✕ 원본 저장 | `:424-430` 자체 쿼리 |
| 웹 일괄 | `DataCollectionHub.razor:596-655` | ✕ | 동일 |
| 모바일 단일·일괄 | `ReceiptCapture.razor:573-773`, `:797-816` | ✕ | `:544-550` **같은 쿼리 복붙** |
| 서비스 | `AccountingService.cs:272-341` `UploadReceiptAsync` | **○ ImageSharp 1200px** | — |

**리사이즈를 하는 유일한 경로(서비스)를 화면 어디서도 쓰지 않습니다.** 실제 업로드는 전부 원본이 저장됩니다.
`FileService`는 경로·파일명 헬퍼만 제공하고 저장 흐름은 세 곳이 각자 조립합니다.

### 6.6 모바일 CSS가 두 체계로 쪼개짐

`wwwroot/mobile-app.css`(329줄)가 `.ma-*` 토큰을 정의하는데, 정작 큰 두 페이지는 **접두사만 바꿔 같은 컴포넌트를
인라인으로 재정의**합니다.

| 체계 | 정의 위치 | 사용 횟수 |
|---|---|---|
| `.ma-*` | `wwwroot/mobile-app.css` (329줄) | 346 |
| `.ml-*` | `MobileLedger.razor:12-250` (**239줄 인라인**) | 141 |
| `.mh-*` | `MobileHome.razor:8-183` (**176줄 인라인**) | 86 |

`.ma-*`를 쓰는 페이지는 `MobileSettings`·`MobileResolution`·`MobilePlan`·`MobileEventReport`·`MobileMore`·`MobileMatching`입니다.
같은 디자인 토큰(색·간격·라운드)이 세 벌 존재하므로, 토큰 하나를 바꾸려면 세 곳을 고쳐야 합니다.

인라인 `<style>` 블록이 있는 razor는 **20개**이고, 큰 것은
`MonthlyLedger`(400줄, 인쇄 CSS 포함), `Home`(252줄), `MobileLedger`(239줄), `MobileHome`(176줄)입니다.

### 6.7 그 밖의 복사·붙여넣기

- **`ShowAlertAsync` 5중 복제** — `MonthlyLedger.razor:1624`, `DataCollectionHub.razor:234`,
  `Settings.razor:2074`, `ExpenseReportQuarterly.razor:876`, `ReceiptCapture.razor:887`(이름만 `AlertAsync`)
- **ClosedXML 엑셀 생성이 5개 파일에 산재** — `AnnualPlanBoard.razor`, `MonthlyLedger.razor`,
  `Settings.razor`, `DataCollectionHub.razor`, `AccountingService.cs` (`XLWorkbook` 사용 기준).
  `리팩토링_진행보고서.md` §7-4가 "`ExcelExportService` 추출"을 미착수로 남긴 항목이며, 당시 3개 파일에서 늘었습니다
- **인쇄 HTML 생성 이중화** — 웹은 `MonthlyLedger.razor:1363-1401`이 `StringBuilder`로 `<style>`을 조립하고
  **`JSRuntime.InvokeAsync<string>("eval", "document.querySelector('.print-only').innerHTML")`로 DOM을 긁어옵니다**(`:1403,1408`).
  모바일은 `/ledger/print` 전용 페이지가 서버에서 같은 표를 다시 렌더합니다.
  `MobileLedgerPrint.razor:43` 주석이 "웹 인쇄와 같은 규칙"이라고 자인하고 있습니다

---

## 7. 데이터 접근 패턴

### 7.1 컴포넌트가 직접 DB를 여는 곳 — 11개 파일 / 43회

| 파일 | `CreateDbContext()` |
|---|---:|
| `Components/Pages/Settings.razor` | **15** |
| `Components/Pages/DataCollectionHub.razor` | **12** |
| `Components/Pages/MonthlyLedger.razor` | 7 |
| `Components/Pages/ReceiptCapture.razor` | 2 |
| `Components/Pages/MobileSettings.razor` | 2 |
| `Components/Pages/Home.razor` | 2 |
| `Components/Pages/Manual.razor` | 1 |
| `Components/Pages/Login.razor` | 1 |
| `Components/Layout/MainLayout.razor` | 1 |
| `Components/Pages/AnnualPlanBoard.razor` | 0 — **주입만 하고 미사용(죽은 의존성)** |
| `Components/Pages/WeeklyMeetingBoard.razor` | 0 — 동일 |

**43회 중 27회(63%)가 `Settings` + `DataCollectionHub` 두 파일**입니다.

### 7.2 서비스만 쓰는 컴포넌트

`MobileHome` `MobileLedger` `MobileLedgerPrint` `MobilePlan` `MobileMore` `MobileMatching` `MobileResolution`
`MobileEventReport` `EventReportBoard` `EventReportEdit` `ExpenseReportQuarterly` — **모바일 화면군이
"얇은 뷰 + 두꺼운 서비스"의 참조 모델 역할**을 하고 있습니다. `MobileHome`은 @code가 92줄뿐입니다.

### 7.3 비일관

- **혼재 페이지 9개** — 같은 파일에서 서비스와 직접 DB를 둘 다 씀 (`Home`, `MonthlyLedger`, `Settings`,
  `DataCollectionHub`, `ReceiptCapture`, `MobileSettings`, `Manual`, `AnnualPlanBoard`, `WeeklyMeetingBoard`)
- **주입 이름 비일관** — `DbContextFactory`(대부분) vs `DbFactory`(`Manual.razor`),
  `Accounting`(대부분) vs `AccService`(`Settings`, `Manual`, `ExpenseReportQuarterly`)

`리팩토링_진행보고서.md` §3이 "DbContext 접근 방식 통일"을 1단계로 완료했다고 기록하는데,
그건 *주입식 DbContext → 팩토리* 전환이었고 *razor → 서비스* 계층 분리는 아닙니다.

---

## 8. 검증 가능성 현황

> 이 절이 리팩토링을 시작하려는 이유("검증할 방법이 없다")에 직접 대응합니다.

### 8.1 지금 있는 것

**없습니다.**

- 테스트 프로젝트·테스트 파일·테스트 프레임워크 패키지 **전부 0건**. `INcheonChurchWeb.sln`에 프로젝트는 1개
- `.csproj`에 xUnit/NUnit/MSTest/**bUnit** 참조 없음
- `.github/workflows/deploy.yml`은 `on: workflow_dispatch` 전용이고 `dotnet test` 스텝이 없음
  → **푸시·PR 시 빌드 검증조차 돌지 않습니다**

### 8.2 지금 실제로 하는 검증 (수동)

1. `dotnet build` — 컴파일 오류·경고
2. 생성된 Razor C#(`obj/gen`)에서 **마크업 오염 검사** — `@if`/`else`/`foreach`가 코드가 아니라 문자열로
   새어 들어갔는지 grep
3. 앱 기동 후 라우트 HTTP 200 확인
4. **로그인 이후의 모든 동작은 사람이 직접 눌러서 확인** — 브라우저 세션이 `ProtectedLocalStorage` 기반이라
   자동화가 어렵습니다

### 8.3 테스트가 없어 실제로 놓친 회귀

빌드는 통과하는데 화면이 깨지는 종류입니다.

| 커밋 | 증상 |
|---|---|
| `5f31845` | `@if`가 화면에 **글자로 렌더링** |
| `d4c7723` | 같은 유형 |
| `f2c7374` | 월 합계 행의 `colspan` 불일치 |

이번 작업 중에도 같은 함정을 한 번 더 밟았습니다 — `else { } else { }`가 컴파일은 통과하고
두 번째 `else`가 마크업 텍스트로 렌더되는 상태였습니다.

### 8.4 지금 당장 테스트로 굳힐 수 있는 것 (순수 함수, DB·파일 불필요)

| 대상 | 위치 | 왜 중요한가 |
|---|---|---|
| 11월 넷째 주 일요일 계산 | `DateHelper.GetFiscalYearEndDate`, `AccountingService.GetFourthSundayOfMonth`(98) | 회계연도 경계 그 자체 |
| 회계연도 판정 | `DateHelper.CalculateFiscalYear`, `AccountingService.CalculateFiscalYearAsync`(1363) | **두 구현이 같은 답을 내는지**가 §6.1 리스크의 핵심 |
| 기본 분기 범위 | `AccountingService.GetDefaultQuarterRange`(115) | 12월/11월 경계 |
| 이월금·잔액 누계 | `AccountingService.GetLedgerViewAsync`(1865) 중 순수 계산부 | §3.5 식 |
| 검색어 파서 | `Models/BoardSearch.cs` 전체 | 이미 순수 함수 — 바로 테스트 가능 |
| 금액 파싱 | `AccountingService.ParseMoney`(1345), 모바일 페이지들의 `ParseWon` | 콤마·원 표기 |
| 항목별 집행률 | `BuildCategoryStats` 2벌 | §6.3의 한 줄 차이를 드러냄 |

### 8.5 테스트가 어려운 것

- EF Core 쿼리 대부분 (인메모리 SQLite로는 가능하나 시드 준비 필요)
- 파일 저장·이미지 리사이즈 (`FileService`, ImageSharp)
- Vision API OCR (외부 의존)
- 인쇄 레이아웃, Blazor 렌더링 (bUnit 도입 필요)
- 웹↔모바일 자동 교체 (브라우저 폭·포인터 판정)

---

## 9. 빌드 · 배포 · 운영

### 9.1 `INcheonChurchWeb.csproj`

`net8.0` / `Nullable: enable` / `ImplicitUsings: enable` / `AllowUnsafeBlocks: true`.

패키지 12개: `ClosedXML 0.105.0`, `CsvHelper 33.1.0`, `ExcelDataReader 3.8.0`(+`.DataSet`),
`Google.Cloud.Vision.V1 3.8.0`, `EntityFrameworkCore.Sqlite 8.0.0`(+`Tools`),
`VisualStudio.Azure.Containers.Tools.Targets 1.22.1`, `Otp.NET 1.4.1`, `QRCoder 1.7.0`,
`SixLabors.ImageSharp 3.1.12`, `System.Text.Encoding.CodePages 10.0.7`.

⚠️ 관찰:
- `System.Text.Encoding.CodePages`가 **10.x** — net8.0 프로젝트에 메이저 버전이 어긋나 있음
- **엑셀 라이브러리 2종 병존**(ClosedXML 쓰기 + ExcelDataReader 읽기)
- **CSV 파싱 2경로 병존** — `CsvHelper` 패키지와 자체 `SplitCsvLine`(`AccountingService.cs:852`)
- `<Content Update="google-vision-key.json" CopyToOutputDirectory="Always">` — **비밀키가 빌드 산출물로 항상 복사**

### 9.2 Docker / CI

- `Dockerfile`은 VS 템플릿 그대로이고 **경로가 현 레포 구조와 맞지 않습니다**
  (`COPY ["INcheonChurchWeb/INcheonChurchWeb.csproj", ...]`, `WORKDIR "/src/INcheonChurchWeb"` —
  csproj가 루트에 있는 지금 구조에서는 빌드 실패). `Dockerfile.original`도 방치되어 있습니다
- 실제 배포는 `.github/workflows/deploy.yml`이 `dotnet publish` 산출물을 **scp로 서버에 올리고**,
  서버의 `docker-compose`가 빌드·재기동하는 흐름입니다
- 트리거는 `workflow_dispatch` **수동 전용**(`push` 트리거는 주석 처리 — 진행보고서 §14-3의 "자동 배포 중단" 결정)
- ⚠️ `dotnet test` 없음 / 액션이 `@master` 핀(공급망 리스크) / `chmod -R 777` / `docker-compose` v1

### 9.3 리포에 커밋된 비밀·산출물 (리팩토링과 별개로 즉시 정리 대상)

`google-vision-key.json`(빌드 산출물로도 복사됨), `keys/*.xml`(DataProtection 키 2개),
`church.db`(164KB) + `church.db.old_*`(148KB), `publish_output/`(DLL 전체 + 키 파일 포함), `bin/`, `obj/`.

### 9.4 정적 파일 캐시

`Components/App.razor`가 정적 자산 주소에 **파일 마지막 수정 시각**을 자동으로 붙입니다
(`/js/app.js?v=639241653514127223` 형태).

이전에는 `?v=1.1` 고정이라 **JS를 고쳐도 브라우저가 캐시된 옛 파일을 계속 실행했습니다.**
인쇄 팝업이 안 닫히던 문제의 실제 원인이 이것이었고, 수정 두 번이 헛돌았습니다.

---

## 10. 함정 모음

### 10.1 코드에 남아 있는 것

| 위치 | 내용 |
|---|---|
| `Data/DbInitializer.cs:15-17` | 레거시 DB는 마이그레이션 **베이스라인 등록 1회 선행 필수**. 안 하면 기동 실패 |
| `Services/PasswordHasher.cs:14` | **평문 비밀번호 폴백이 살아있음** — 오래 로그인하지 않은 계정은 평문일 수 있음 |
| `Services/AccountingService.cs:83-86` | Blazor Server의 Scoped `DbContext`는 circuit 수명 동안 살아남아 **비동기 작업 간 스레드 충돌**. 그래서 `IDbContextFactory`를 씀 — razor 11개 파일도 같은 규칙을 따라야 함 |
| `Services/AccountingService.cs:592-599` | 1회성 레거시 이관. 멱등성 보장, 금액 이상치는 Quarantine 격리, 구분 판정은 Type 문자열보다 **"금액이 든 컬럼"을 1차 근거**로 신뢰 |
| `Components/Pages/MonthlyLedger.razor:282-303` | 인쇄 **백지 현상**을 막으려고 Blazor 레이아웃 뼈대를 CSS로 강제 해체하는 3단계 트릭 |
| `Components/Pages/MonthlyLedger.razor:1329` | `_selectedPrintType` → `_printMode` **수동 동기화** 필요 |
| `Components/Pages/Settings.razor:1454`, `:1770` | ⚠️ "최고관리자 권한으로 저장 시 **모든 부서의 회계 분기 일정이 일괄 변경**" — 같은 경고가 두 곳에 복붙 = 같은 위험 동작이 두 경로 |
| `Components/Pages/DataCollectionHub.razor:882`, `:957` | 중복 장부 내역 발견 시 **삭제 후 덮어쓰기** — 파괴적 경로 |
| `Services/OcrService.cs:25-41` | 날짜·금액 **키워드 배열 순서가 곧 우선순위**. 짧은 '일자'는 오탐이 많아 뒤로 뺌 |
| `Components/Layout/MainLayout.razor` | 세션은 **User 객체가 아니라 Username 문자열만** 저장하고 DB에서 재조회 (직렬화 깨짐 회피) |

### 10.2 문서에 이미 정리된 것 (원문 참조)

- **Blazor/Razor·ClosedXML·DB/배포 작업 수칙** → `생명주기_설계메모.md` §7
  (`@` 뒤 한글은 `@(...)`, CSS `@media`는 `@@media`, 모달 배경 `@onclick` 금지,
  테이블 열 추가 시 `<th>`/`<td>`/`<col>`/`colspan` 넷 다, 배포 후 컨테이너 교체 확인 등)
- **SQLite 트랜잭션 함정** → `예산_분류체계_개선안.md` §6
  (`COMMIT;` 없으면 조용히 롤백되는데 스크립트 안 검증 쿼리는 성공한 것처럼 보임 — 두 번 속았음)

### 10.3 이번에 새로 확인된 것

- **정적 파일 캐시** — §9.4. 고정 버전 태그 때문에 JS 수정이 반영되지 않음
- **`else { } else { }`가 컴파일을 통과함** — 두 번째 `else`가 마크업 텍스트로 렌더됨.
  생성 C#을 뽑아 검사하지 않으면 못 잡음
- **`IBrowserFile.OpenReadStream()`은 파일당 한 번만** — OCR과 저장이 각각 열면 두 번째에서 예외.
  영수증 전송 실패의 원인이었음

---

## 11. 부록

### 11.1 파일 규모 (500줄 초과)

| 줄수 | 파일 |
|---:|---|
| 2239 | `Services/AccountingService.cs` |
| 2091 | `Components/Pages/Settings.razor` |
| 1625 | `Components/Pages/MonthlyLedger.razor` |
| 1218 | `Components/Pages/DataCollectionHub.razor` |
| 1150 | `Components/Pages/ExpenseReportQuarterly.razor` |
| 1147 | `Components/Pages/AnnualPlanBoard.razor` |
| 998 | `Components/Pages/MobileLedger.razor` |
| 888 | `Components/Pages/ReceiptCapture.razor` |
| 808 | `Components/Pages/Home.razor` |
| 657 | `Components/Pages/Manual.razor` |
| 650 | `Components/Pages/MobilePlan.razor` |
| 569 | `Components/Pages/WeeklyMeetingBoard.razor` |
| 513 | `Components/Pages/MobileHome.razor` |

### 11.2 용어

| 용어 | 뜻 |
|---|---|
| 분류항목 (`Category`) | 지출은 **행사**(왜 썼나), 수입은 회비수입·찬조금 등 |
| 세부 (`SubCategory`) | 지출은 **비용 종류**(무엇에 썼나), 수입은 **행사명** |
| 증빙 | 거래에 연결된 영수증 사진 (`ReceiptPath` 또는 `ReceiptUrl`) |
| 전월 이월금 | 회계연도 기준으로 앞선 기간에서 넘어온 잔액 (§3.5) |
| 지출결의서 (`ExpenseReport`) | 교회 재정부에 보조금을 청구하는 문서. **승인 절차 없음** |
| 행사 보고서 (`EventReport`) | 행사 사후 기록. 재정 수치는 저장하지 않고 장부에서 매번 집계 |
| 매칭 | 업로드된 영수증(`UploadedReceipt`)을 장부 행에 연결하는 작업 |
