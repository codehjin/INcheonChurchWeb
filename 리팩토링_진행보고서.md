# 코드 리팩토링 진행 보고서

> 작성일: 2026-06-19
> 대상: `INcheonChurchWeb` 전체 프로젝트
> 원칙: 비즈니스 로직 · 화면 UI 기능 무손상, 내부 코드 구조만 일관성 정리

---

## 1. 코드 분석 결과 — 가장 시급한 코드 스멜 3가지

### 1️⃣ DbContext 접근 방식 불일치 *(가장 위험)*

DI는 **팩토리만** 등록되어 있음: `AddDbContextFactory<AppDbContext>` ([Program.cs:71](Program.cs#L71)).
그런데 화면마다 사용 방식이 갈렸음.

| 패턴 | 파일 |
|---|---|
| ✅ `using var context = DbContextFactory.CreateDbContext()` (안전) | `MonthlyLedger.razor`, `DataCollectionHub.razor` |
| ⚠️ `@inject AppDbContext DbContext` (스코프 컨텍스트 직접 주입) | `Home.razor`, `Settings.razor`, `MainLayout.razor`, `Login.razor` |

**위험성:** Blazor Server에서 주입식 `AppDbContext`는 서킷 수명 동안 공유되어,
동시 렌더링/이벤트 시 *"A second operation started on this context"* 예외가 발생할 수 있음.

### 2️⃣ 명명 규칙 혼재

한 파일 안에서도 규칙이 섞여 있음.
- **private 필드가 PascalCase** (규칙은 `_camelCase`): `CurrentYear`, `IsAdmin`, `DashboardDeptId`, `SubsidyBudget` (Home) / `CurrentQuarter`, `SelectedDepartmentId` (MonthlyLedger)
- **같은 파일에 camelCase 필드도 공존**: `isLoading` (MonthlyLedger)
- **로컬 변수가 PascalCase**: `using var DbContext = ...` (MonthlyLedger)

### 3️⃣ 중복 보일러플레이트

- **엑셀 내보내기 로직**(`new XLWorkbook()` → `SaveAs(stream)` → `ToBase64String` → JS 다운로드)이 **3개 파일에 12회** 중복 (DataCollectionHub 4 / Settings 5 / MonthlyLedger 3)
- **`using var context` + try/catch + `JSRuntime.InvokeVoidAsync("alert", ...)`** 에러 처리 패턴이 DataCollectionHub에서만 수십 회 반복
- 서비스 파일명 오타 `AccountingServics.cs` (클래스는 `AccountingService`)

---

## 2. 진행 방식 (단계 구분)

대상이 프로젝트 전체라 한 번에 고치면 검증·기능 손상 위험이 커서 **단계별**로 진행.

1. **1단계 (안전성):** DbContext 접근 방식 통일 — *완료*
2. **2단계 (가독성):** 파일별 명명 규칙(#2) 통일 + 엑셀/에러 헬퍼 추출(#3) — *예정*

---

## 3. ✅ 1단계 작업 완료 — DbContext 접근 방식 통일

### 수정 내용
- `@inject AppDbContext DbContext` → `@inject IDbContextFactory<AppDbContext> DbContextFactory`
- DB 접근 메서드마다 `using var context = DbContextFactory.CreateDbContext();`로 스코프 분리
- 기존 비동기 호출 / 에러 처리 / `SaveChangesAsync` 흐름은 그대로 유지, `DbContext.` 참조만 `context.`로 변경

### 대상 파일 및 적용 범위

| 파일 | inject 교체 | `using var context` 적용 메서드 수 |
|---|---|---|
| `Components/Layout/MainLayout.razor` | ✔ | 1곳 (유저 복구) |
| `Components/Pages/Login.razor` | ✔ | 1곳 (로그인) |
| `Components/Pages/Home.razor` | ✔ | 2곳 (`OnInitializedAsync`, `LoadDashboardData`) |
| `Components/Pages/Settings.razor` | ✔ | 15곳 (LoadData · 임원관리 · 백업/복원 · 엑셀 · 2FA 등 DB 접근 메서드 전체) |

### 기능 영향 분석
각 메서드가 독립적으로 조회→수정→저장하며 메서드 간 추적(tracking) 엔티티를 공유하지 않아,
컨텍스트 분리로 인한 로직 변화 없음.
(MonthlyLedger · DataCollectionHub는 이미 팩토리 패턴이라 미수정.)

### 빌드 검증
`dotnet build` 결과 **오류 0개**. 남은 경고 4개는 이번 작업과 무관한 기존 항목
(`IsAdmin` 미사용 필드, nullable 역참조 경고 등).

### 참고 (사소)
`Settings.LoadData`는 탭에 따라 DB를 쓰지 않는 분기(백업 탭 등)에서도 `context`를 생성함.
무해하지만 2단계에서 분기 안쪽으로 옮겨 불필요한 생성을 줄일 수 있음.

---

## 4. 다음 단계 (2단계) 결정 대기 항목

- **진행 단위:** ① 파일 우선(권장) / ② 규칙 우선 / ③ #3(엑셀 헬퍼)만 먼저
- **착수 전 별도 확인 필요:**
  - #3 공용 헬퍼 위치 (예: `Services/ExcelExportService.cs` 신규)
  - 큰 파일의 Partial Class(`*.razor.cs`) 분리 여부

---

## 5. ✅ 2단계 작업 — 파일 단위 리팩토링 (#2 명명 규칙 + #3 보일러플레이트)

진행 방식: **파일 우선** — 한 파일씩 #2/#3 적용 후 `dotnet build` 검증.

### 5-1. `Home.razor` — 완료
- private 필드 13개 `_camelCase` 변경(예: `CurrentYear`→`_currentYear`, `IncomeStats`→`_incomeStats`), 마크업 `@` 참조 동기화.
- 프로퍼티(`RemainingBudget`)·메서드는 PascalCase 유지.
- 미사용 필드 삭제: `IsAdmin`(CS0414), `TotalIncomeBudget`(선언·계산만 하고 미사용인 죽은 필드).
- 중복되던 수입/지출 통계 생성 블록(34줄)을 공용 헬퍼 `BuildCategoryStats(...)`로 통합(결과값 동일).
- 빌드 오류 0, 경고 4→3(IsAdmin 경고 제거).

### 5-2. `MonthlyLedger.razor` — 완료
- private 필드 36개 전부 `_camelCase` 변경, `@bind`/`@onclick`/`@if`/`@foreach` 바인딩 동기화.
- 프로퍼티(`CurrentBalance`, `IsAllEditSelected`)·메서드(`ShowReceiptModal` 등) PascalCase 유지.
- 로컬 변수 `DbContext` → `context`로 통일(팩토리 패턴 컨벤션 일치).
- 반복 alert 9곳을 `ShowAlertAsync(string)` 헬퍼로 압축(`confirm`/`printPopup`/`eval`/`triggerFileInput`/`downloadExcelFile`은 무변경).
- CS8602(1152) 해결: `CurrentUser.DepartmentId` → `(CurrentUser?.DepartmentId ?? 0)`.
- 절대 보존: 인쇄 CSS(`@media print`)·HTML 구조·인라인 스타일·권한 변수(`CanViewAll`/`CanEditLedger`)·비즈니스 계산 무변경. 줌/이동 로직은 외부 JS라 영향 없음.
- 빌드 오류 0, 경고 3→2(MonthlyLedger CS8602 제거). 엑셀 로직은 구조 유지(네이밍/alert만 적용).

### 5-3. `Settings.razor` — 완료
- private 필드 40개 전부 `_camelCase` 변경, 마크업 바인딩(`@bind`/`@if`/`@foreach`/`@key` 등) 동기화.
- 프로퍼티(`CurrentYearBudgets`, `GroupedBudgets`, `ManualBackups`, `AutoBackups`)·메서드 PascalCase 유지.
- 반복 alert 30곳을 `ShowAlertAsync(string)` 헬퍼로 압축(`confirm`/`eval`/`downloadJsonFile`/`downloadExcelFile`은 무변경).
- `LoadData`의 불필요한 `context` 생성 제거: 항상 생성하던 것을 DB가 필요한 3개 분기(Income·Expense / Mapping / Officers·Account)로 한정. Users·Backup 탭은 더 이상 context 미생성.
- CS8602 2건 근본 해결:
  - 1020행: EF 식 트리(`FirstOrDefaultAsync`) 안에서는 `?.` 사용 불가(CS8072) → 사용자명을 쿼리 밖 지역변수(`var username = CurrentUser?.Username;`)로 추출 후 비교.
  - 1111행: 인메모리 `List.Where` 람다라 `(CurrentUser?.DepartmentId ?? 0)`로 안전 처리.
- 절대 보존: HTML 레이아웃·외부 JS 호출 인자(파일 다운로드 등)·백업/엑셀 템플릿/2FA(OTP) 비즈니스 로직 무변경.
- 빌드 **경고 0 / 오류 0** 달성.

### 5-4. `DataCollectionHub.razor` — 완료
- private 필드 약 50개 `_camelCase` 변경, 자식 컴포넌트(`<ReceiptSingleUpload>`·`<ReceiptMultiUpload>`·`<BankImport>`·`<LedgerMatching>`) 파라미터 바인딩 **RHS 값만** 갱신(`ImgScale="@_imgScale"` 식). 자식 파라미터명(LHS)·`@bind` 대상·콜백 메서드명은 보존.
- 자식 파라미터명과 동일했던 PascalCase 필드(`IncomeCats`/`ExpenseCats`/`LastTransactionDate`/`SearchKeyword`)는 2단계(일괄 치환 후 파라미터 LHS 복원)로 안전 처리.
- 서비스 메서드명 부분 일치 충돌 1건 수정: `LastTransactionDate` 치환이 `GetLastTransactionDateAsync`를 깨뜨려 복원(`GetDepartmentsAsync` 류 동일 패턴).
- 반복 alert 30곳을 `ShowAlertAsync(string)` 헬퍼로 압축(`confirm`/`downloadExcelFile`/`triggerFileInput` 등은 무변경).
- 절대 보존: 권한 분기(`CurrentUser?.CanAccessExcelTab`, `CanViewAll` 프로퍼티)·탭 전환 방어 로직, 줌/팬(Zoom&Pan) 이벤트 핸들러·바인딩, 엑셀 다운로드/업로드·드래그앤드롭 JS 호출 인자 무변경. `const`(MaxFileSize 등)·정적필드(`_savedPreviewList`)는 관례 유지.
- 빌드 **경고 0 / 오류 0**.

---

## 6. 남은 대상 (예정)

### 6-1. 2단계(명명/보일러플레이트) 미적용 파일
- [x] `Components/Pages/Login.razor` — **완료**(2026-06-20). private 필드 10개 `_camelCase` 변경(`Username`→`_username` 등), `@bind`/`@onclick`/`@if`/`disabled` 마크업 동기화. EF 엔티티 속성(`u.Username`/`u.Password`)·파라미터(`OnLoginSuccess`)·메서드명 보존. alert 미사용 파일이라 #3 해당 없음. 빌드 경고 0/오류 0.
- [x] `Components/Layout/MainLayout.razor` — **완료**(2026-06-20). private 필드 2개 변경(`CurrentUser`→`_currentUser`, `isLoaded`→`_isLoaded`), 마크업·`<CascadingValue Value>` RHS 동기화. `<NavMenu CurrentUser="_currentUser">` 자식 파라미터명(LHS) 보존. 빌드 경고 0/오류 0.
- [x] `Components/Pages/ExpenseReportQuarterly.razor` — **완료**(2026-06-20). 일반 private 필드 11개 `_camelCase` 변경(`TargetYear`→`_targetYear`, `CurrentReport`→`_currentReport`, `itemList`→`_itemList`, `newItem`→`_newItem`, `isEditing`→`_isEditing`, `editingItem`→`_editingItem`, `TotalBudget`/`UsedBudget`/`inputAmount`/`reportHistory`/`catList` 등), 마크업 `@bind`/`@foreach`/`@onclick`/`@(...)` 바인딩 동기화. 프로퍼티(`CurrentRequestTotal`·`AmountInKorean`)·`CurrentUser`(CascadingParameter)·메서드·로컬변수 보존. alert 4곳을 `ShowAlertAsync` 헬퍼로 압축(`confirm` 2곳·`eval` 인쇄 호출 무변경). **절대 보존**: 135mm×195mm 인쇄 CSS(`@media print`·`.expense-paper`·가위선)·인라인 스타일·보조금 합산/저장/Role 필터 로직 전부 무변경. 빌드 경고 0/오류 0.

### 6-2. 자식 컴포넌트 (DataCollectionHub에서 호출) — 완료(2026-06-20)
- [x] `Components/Pages/BankImport.razor` — 일반 private 필드 `draggedItem`→`_draggedItem`(선언+사용 5곳, 마크업 `@ondrop` 분기 포함) 변경. `[Parameter]` 25개·메서드명·`@bind:get/set` 콜백 전부 보존. alert 호출 없음.
- [x] `Components/Pages/ReceiptSingleUpload.razor` — **변경 없음**. 모든 멤버가 `[Parameter]` 또는 메서드라 대상 일반 필드 없음, alert 없음.
- [x] `Components/Pages/ReceiptMultiUpload.razor` — **변경 없음**. 위와 동일(표현용 컴포넌트).
- [x] `Components/Pages/LedgerMatching.razor` — **변경 없음**. 위와 동일(표현용 컴포넌트).
> ⚠️ 부모(`DataCollectionHub`)와 파라미터명을 공유하므로, 자식의 `[Parameter]` 프로퍼티명(LHS)은 변경하지 않음 — RHS 일반 필드만 정리. 빌드 경고 0/오류 0.

### 6-3. 기타 페이지/레이아웃
- [ ] `Components/Pages/Manual.razor`
- [ ] `Components/Pages/Error.razor`
- [ ] `Components/Layout/NavMenu.razor`, `EmptyLayout.razor`
- [ ] `Components/App.razor`, `Routes.razor`, `_Imports.razor`

### 6-4. 서비스/모델 (C#) — 분석 완료 + 안전 변경 2건 완료(2026-06-20)
- [x] `Services/AccountingServics.cs` → **`AccountingService.cs`로 파일명 오타 수정**(`git mv`, 이력 보존). 클래스명은 기존부터 `AccountingService`라 코드 변경 0줄·DI/주입 무영향(csproj SDK globbing). 내부 필드(`_db`/`_env`)·메서드 명명은 이미 규칙 준수.
- [x] `Services/OcrService.cs` — private 필드 2개 `_camelCase` 변경(`DateKeywords`→`_dateKeywords`, `AmountKeywords`→`_amountKeywords`). 정규식/추출 로직 무변경.
- [x] `Services/AutoBackupService.cs`, `FileService.cs`, `TwoFactorAuthService.cs` — **점검 완료, 변경 불필요**(이미 규칙 준수, AutoBackup은 싱글턴 호스티드라 `CreateScope()` 사용이 올바름).
- [x] `Models/*` — **점검 완료, 변경 금지 확정**. public 프로퍼티 PascalCase는 EF 엔티티 규약상 정상이며, 프로퍼티명=DB 컬럼명 + `ExpenseReport.DetailsJson` JSON 직렬화 호환성 때문에 변경 시 데이터 파손 위험. 손대지 않음.
- [ ] ⚠️ **보류**: `AccountingService`의 `AppDbContext` 직접 주입(서비스층 1단계 스멜) — EF 변경추적 의존 메서드 다수로 전환 위험이 커, 명명 패스에서 배제. 추후 별도 회귀 테스트와 함께 검토.

### 6-5. 공통 후속(전 파일)
- [ ] #3 엑셀 내보내기 로직: 3개 파일(Settings/MonthlyLedger/DataCollectionHub) 공통 헬퍼(`Services/ExcelExportService.cs`)로 추출 — *별도 승인 후 진행*
- [ ] 큰 파일 Partial Class(`*.razor.cs`) 분리 검토 — *별도 승인 후 진행*

### ✅ 진행 현황 요약
| 파일 | 1단계(DbContext) | 2단계(명명/#3) |
|---|---|---|
| Home.razor | ✅ | ✅ |
| MonthlyLedger.razor | (이미 팩토리) | ✅ |
| Settings.razor | ✅ | ✅ |
| DataCollectionHub.razor | (이미 팩토리) | ✅ |
| MainLayout.razor | ✅ | ✅ |
| Login.razor | ✅ | ✅ |
| ExpenseReportQuarterly.razor | (서비스주입) | ✅ |
| 자식 컴포넌트 4종 | (해당없음) | ✅ |

---

## 7. 🏁 최종 총평 (2026-06-20 리팩토링 완료)

2026-06-19 착수한 전체 리팩토링이 **무결성 손상 0건, 빌드 경고/오류 0**으로 완료되었다.
> 본 §7은 **명명/보일러플레이트 리팩토링** 범위의 총평이며, 이후 진행된 후속 작업(반응형 UI·인코딩 복구·아이콘)은 아래 **§8~§10** 참고.

### 7-1. 완료 범위
| 영역 | 결과 |
|---|---|
| **화면/레이아웃 razor** (Home·MonthlyLedger·Settings·DataCollectionHub·MainLayout·Login·ExpenseReportQuarterly) | 1·2단계 전부 완료 |
| **자식 컴포넌트 4종** (BankImport·ReceiptSingleUpload·ReceiptMultiUpload·LedgerMatching) | 2단계 완료(표현용이라 BankImport `_draggedItem` 1건 외 변경 불필요) |
| **C# 서비스** | 파일명 오타 수정(`AccountingService.cs`) + `OcrService` 필드 2건. 나머지 서비스는 이미 규칙 준수 |
| **C# 모델** | 점검 후 **의도적 보존**(EF 컬럼 매핑·JSON 호환성) |

### 7-2. 적용한 일관성 원칙
- **명명(#2)**: private 일반 필드 = `_camelCase` / 프로퍼티·메서드 = PascalCase / 로컬변수 = camelCase / `[Parameter]`·EF 프로퍼티 = 불변(외부 계약).
- **보일러플레이트(#3)**: 반복 `alert` 호출 → `ShowAlertAsync` 헬퍼(MonthlyLedger·Settings·DataCollectionHub·ExpenseReportQuarterly). `confirm`/`eval`/파일 다운로드 JS 인자는 무변경.
- **안전성(1단계)**: 주입식 `AppDbContext` → 팩토리(`using var context`) 통일(razor 한정).

### 7-3. 절대 보존 검증 항목 (무손상 확인)
- 지출결의서 **135mm×195mm 인쇄 규격 CSS**(`@media print`·가위선·`.ep-*`)·인라인 스타일
- 보조금 합산/예산 검증/저장 흐름, Role 기반 권한 분기, 드래그앤드롭·줌팬·OCR·파일 업로드 로직
- 자식↔부모 `[Parameter]` 바인딩 계약(LHS 불변, RHS만 동기화)

### 7-4. 남긴 후속 과제 (별도 승인 필요)
- **[보류] `AccountingService`의 `AppDbContext` 직접 주입** — 서비스층 1단계 스멜이나, EF 변경추적 의존 메서드가 많아 아키텍처 전환 위험 큼. 회귀 테스트 동반 별도 진행 권장.
- **[미착수] 엑셀 내보내기 공통 헬퍼**(`Services/ExcelExportService.cs`) 추출 — 3개 파일 12회 중복.
- **[미착수] 대형 파일 Partial Class(`*.razor.cs`) 분리** 검토.

---

## 8. ✅ 반응형 UI 최적화 — 갤럭시 Z 폴드 커버/메인 화면 (2026-06-20, `bea0b41`)

좁은 커버 화면(<350px)에서 테이블이 세로로 무너지고, 폴드 메인(768~991px)에서 그리드가 어색해지는 문제를 핀포인트 수정. **전략: 기존 `overflow-x` 컨테이너를 살려 안쪽 표에 `min-width`를 부여 → 가로 스크롤 발동.**

| 파일 | 변경 | 비고 |
|---|---|---|
| `Settings.razor` (A) | `.settings-table`에 `min-width: 720px` | 8개 표 일괄 해결 |
| `Home.razor` (B) | `.dash-list-wrap` `overflow:hidden`→`overflow-x:auto`, 표 `min-width:560px`, 헤더/작성일/금액/입금일 셀 `white-space:nowrap`(제목 제외) | 세로 무너짐 해소 |
| `Home.razor` (F) | 대시보드 그리드 `minmax(280px,1fr)`→`minmax(min(100%, 260px), 1fr)` ×2 | 초협소 화면 오버플로 방지 |
| `LedgerMatching.razor` (C) | 우측 데이터표에 `min-width:560px` | 기존 래퍼 스크롤 발동 |
| `LedgerMatching.razor` (D) | `col-lg-5/7`→`col-md-5 col-lg-5` / `col-md-7 col-lg-7` | 768~991px에서도 2열 분할 유지 |
| `BankImport.razor` (E) | 래퍼에 `overflow-x:auto` + 표 `min-width:760px` | 8열 표 가로 스크롤 |
| `MonthlyLedger.razor` | **변경 불필요(이미 충족)** | 툴바 `d-flex flex-wrap gap-2/3`, 표 `min-width:1100px`+`overflow-x` 확인 |

- ⛔ **절대 보존**: 모든 `@media print` 블록·인쇄 규격(`.expense-paper` 135mm×195mm, `.ledger-table-print`)은 화면용 클래스와 분리되어 **무변경**.
- **유령 중괄호 `}` 조사**: `LedgerMatching.razor`·`MonthlyLedger.razor` 마크업 전수 추적 결과 **짝 없는 `}`는 소스에 존재하지 않음**(마크업 본문 중괄호 누적 균형 정확히 0, 음수 미발생). 빌드 0/0. → Hot Reload 잔상/구 빌드 산출물 가능성으로 안내(임의 삭제 시 빌드 손상되므로 미변경).
- **빌드**: 0 Warning / 0 Error.

## 9. ✅ 한글 인코딩 손상 복구 (2026-06-20, `6a7d897`)

어제 리팩토링 2단계(`12577fe`)에서 CP949 파일을 UTF-8로 저장하며 일부 한글이 깨진 문제를 복구. **진단 결과 파일별로 상태가 3종류로 갈림.**

| 구분 | 파일 | 상태 | 처리 |
|---|---|---|---|
| 실제 손실 | `Settings.razor` | UTF-8이나 U+FFFD 4454개 + 모지바케 혼재 | **git 복구 + 정밀 병합** |
| CP949(손실 아님) | `BankImport`·`LedgerMatching`·`ReceiptSingleUpload`·`ReceiptMultiUpload` | 한글 바이트 보존, CP949 저장이라 깨져 보임 | **iconv CP949→UTF-8** |
| 정상 | `DataCollectionHub.razor` | UTF-8(BOM), 한글 668단어 정상 | 작업 불필요 |

- **Settings 복구 방식**: 손상 직전 `ac8cddb`(06-10) 버전을 iconv로 추출(한글 890단어 무결) → 현재(리팩토링됨) 파일의 **ASCII는 그대로 두고**, `difflib` 줄 정렬 후 **손상된 비ASCII 런만** 원본 한글로 교체. 308줄 중 **297줄 자동 복구**(런 개수 100% 일치) + @code 영역 **10줄 수동 복구**(`"성공적으로 교체되었습니다!"`, `b.Type == "지출"/"수입"`, `d.Name != "관리자" && "시스템"` 등). 결과 U+FFFD **0개**, 한글 899단어, 1540줄 구조 유지.
- ⛔ **리팩토링 100% 보존 검증**: 복구본에서 `min-width:720px`, `_camelCase`/`ShowAlertAsync`/`IDbContextFactory` 마커 69개, `_draggedItem`×6 등 유지 확인(구조상 현재 ASCII만 유지·비ASCII만 교체).
- **빌드**: 0 Warning / 0 Error. 전체 소스 스캔: U+FFFD 0 / CP949 잔존 0 — 전부 UTF-8.

## 10. ✅ Settings 깨진 이모지 → Bootstrap 아이콘 교체 (2026-06-20, `ed0fdce`)

§9에서 확인된 대로, 탭/버튼/제목의 이모지(👥·💰 등)는 `ac8cddb` 시점에 이미 CP949가 인코딩 못 해 `?`/`??`로 소실된 상태(한글 복구 범위 밖) → Bootstrap 아이콘으로 시각 복원.

- **UI 13곳 아이콘 삽입**: 탭 9개(`bi-people-fill`·`bi-graph-up-arrow`·`bi-graph-down-arrow`·`bi-magic`·`bi-calendar3`·`bi-hdd-fill`·`bi-shield-lock-fill`·`bi-person-badge`·`bi-bank`), 제목/버튼 4개(`bi-tags-fill`·`bi-key-fill`·`bi-link-45deg`, 예산 추가/수정 버튼은 조건부 `bi-plus-circle`/`bi-pencil-square`).
- **찌꺼기 제거**: 주석 3곳 + alert/confirm 메시지 9곳의 `?`/`??` 정리. ⚠️ SweetAlert `formatMessage()`가 `<`를 이스케이프하므로 **메시지 문자열엔 `<i>` 태그 미사용**(텍스트 노출 방지), 찌꺼기만 제거.
- ⛔ **무변경**: C# 로직/바인딩(`_activeTab`·`_newPlan.Id`·삼항 조건), 반응형 CSS(`min-width:720px`).
- Bootstrap Icons 1.11.3 CDN 로드 확인([App.razor](Components/App.razor)), **빌드 0/0**.

---

## 11. ✅ 논리 버그 3건 수정 + DbContext 동시성 전수검사 (2026-06-20)

테스트 중 발견된 3가지 논리 버그를 수정하고, 그 과정에서 §6-4·§7-4에 남겨둔 후속 과제를 **해소/정정**했다.

### 11-1. Google Vision API 사용량 카운트
- [OcrService.cs](Services/OcrService.cs) — `DetectTextAsync` 성공 직후 `IncrementUsageAsync()` 호출. 팩토리로 단명 컨텍스트를 열어 이번 달(`yyyy-MM`) `OcrUsages` 행을 `UsageCount++`(없으면 신규 생성) 후 `SaveChangesAsync`. 집계 실패가 OCR을 막지 않도록 예외는 삼킴.
- [DataCollectionHub.razor](Components/Pages/DataCollectionHub.razor) — 단일(`HandleFileSelected`)·다중 업로드 모두 처리 후 `LoadOcrUsage()`로 화면 카운트 갱신. → **검증 완료, 정상.**

### 11-2. 인코딩 찌꺼기(`?`) 데이터 정화
- **DB측**: [DbInitializer.cs](Data/DbInitializer.cs) `CleanCorruptedDepartmentData()` — 초기화 시점 부서명/은행명/계좌번호/예금주 앞뒤 찌꺼기(`?`·`�`·전각공백)를 `Trim`으로 1회 보정 후 변경분만 `SaveChanges`. (`????? 영유아부` → `영유아부`)
- **마크업측(실제 화면 증상 원인)**: 화면의 "????? 영유아부"는 DB가 아니라 **마크업에 박힌 리터럴**이 원인이었음. [Settings.razor](Components/Pages/Settings.razor) 임원 탭의 `?????`(733·739행)·취소버튼 `?`(800행)를 `bi-people-fill`·`bi-x-lg`로 교체(§10 이모지 교체에서 누락된 분).

### 11-3. DbContext 동시성 전수검사 — 후속 과제 해소
- **[해소] `AccountingService` 팩토리 전환** (§7-4의 보류 항목): 전역 `AppDbContext _db` 주입을 제거하고 생성자를 `IDbContextFactory<AppDbContext>`로 교체, 모든 메서드를 `using var db = _dbFactory.CreateDbContext()` 단명 패턴으로 리팩토링. 잔여 `_db.` 참조 0건 확인.
- **[정정·결함수정] `AutoBackupService.cs`** (§6-4에서 "변경 불필요"로 판정했던 항목 정정): [48행](Services/AutoBackupService.cs#L48)이 `scope.ServiceProvider.GetRequiredService<AppDbContext>()`를 호출했으나 DI에는 팩토리만 등록되어 `AppDbContext`가 Scoped로 **미등록** → 야간 자동백업 실행 시 DI 예외로 크래시하던 잠복 결함. `dbFactory.CreateDbContext()`로 수정(`CreateScope()`로 `AccountingService`를 받는 흐름은 유지).
- **전수검사 결과**: 나머지 소비자(MainLayout·MonthlyLedger·Home·Login)는 이미 팩토리 사용. 프로젝트 전체에서 `AppDbContext` 직접 주입 **0건** 확인.
- **빌드**: 0 Warning / 0 Error.

---

## 12. ✅ 부서 계좌 아이콘 교체 + 자동분류 추천 시스템 (2026-06-20)

### 12-1. 부서 계좌번호 `??` → Bootstrap 아이콘 (§10 후속 누락분)
- [Settings.razor](Components/Pages/Settings.razor) 계좌 탭 부서명 앞 `??`(855·862행, 아코디언/단일카드 두 분기)를 임원 탭과 **동일한** `bi-people-fill`로 교체.

### 12-2. '규칙 분석(추천 시스템)' 신규 기능
자동분류 설정 탭 하단에 기간별 지출 내역을 학습해 자동분류 규칙을 제안하는 섹션 추가.

**DTO 2종** ([AccountingService.cs](Services/AccountingService.cs))
- `CategoryFrequency` : `Category` / `Count` / `Percentage(0~100)`
- `KeywordSuggestion` : `Keyword` / `TotalCount` / `RecommendedCategory` / `RecommendedPercentage` / `Distribution(List<CategoryFrequency>)`

**분석 로직** — `AnalyzeMappingSuggestionsAsync(deptId, year, quarter, month)`
- 단명 컨텍스트(`using var db`)로 선택 기간(`FiscalYear`+`Quarter`+`Date.Month`, 0이면 전체)의 **지출**(`Type=="지출"||"Expense"`) 장부 조회.
- 키워드는 **내역(Description) 우선, 비면 비고(Note)**. `미분류`·빈 분류는 학습 제외.
- `GroupBy(키워드)` → 내부 `GroupBy(분류)`로 빈도/비율 계산 → **최다 비율 항목을 추천 분류로 선정**, 등장 빈도 내림차순 정렬.

**UI 렌더링** (Bootstrap 반응형)
- **필터**: 연/분기/월 `<select>` + `[새로고침(분석 실행)]` 버튼을 `row > col-6 col-md-3`로 배치(Fold 2열·데스크톱 4열).
- **결과표**: `settings-table-container`(가로 스크롤) + `min-width:760px`. 분포는 한 줄 `<div class="progress">` 안에 분류별 `progress-bar` **누적(stacked)**, 너비=`Percentage%`(InvariantCulture로 소수점 안전), 7색 팔레트 순환(`BarColor`).
- **등록**: `[규칙에 추가]` → `SaveMappingAsync(Add→SaveChangesAsync)` 후 `LoadData()`로 상단 지출 규칙 목록 즉시 갱신. 이미 등록된 키워드는 버튼 대신 `등록됨` 배지로 중복 방지.
- ⚠️ Razor가 한글을 식별자 연속문자로 파싱하는 문제(`@y년`→오류)는 `@(y)년`·`@(mo)월`·`@(d.Count)건`로 괄호 구분 처리.
- **빌드**: 0 Warning / 0 Error.

### 12-3. 추천 로직 고도화 — 단어(Token) 기반 키워드 추출 (2026-06-20)

**문제점 (Exact Match의 한계)**
- 초기 `AnalyzeMappingSuggestionsAsync`는 `GroupBy(e => e.Description)`로 **문장 전체(100% 일치)** 를 키로 묶었음.
- `"파리바게트 간식"`과 `"파리바게트(미추홀)"`처럼 한 글자만 달라도 별개 그룹이 되어, **300건의 지출 내역 중 8건만 추천**되는 과소추출 문제 발생.

**해결책 — 4단계 토큰화 파이프라인** ([AccountingService.cs](Services/AccountingService.cs) `Tokenize()` + 본문)

| 단계 | 처리 | 구현 |
|---|---|---|
| ① 특수기호 정제 | 한글·영문·숫자가 아닌 모든 문자(`( ) [ ] , . - _` 등)를 **공백으로 치환** | `char.IsLetterOrDigit(c) ? c : ' '` |
| ② 분리·필터링 | 공백 기준 분리 후 **1글자 단어 + 불용어 제외** | `Split(' ', RemoveEmptyEntries)` → `Where(w.Length >= 2)` → 불용어(`지출`·`결제`·`구입`·`비용`·`이체`·`송금`·`대금`…) `HashSet` 필터 |
| ③ 평탄화(Flattening) | 각 건의 유효 단어들을 그 건의 **분류(Category)와 1:1 매핑** | `SelectMany(x => Tokenize(x.Text).Distinct().Select(token => (token, x.Category)))` (건별 `Distinct`로 한 내역 내 중복 단어는 1표만) |
| ④ 노이즈 제거 | 우연히 1회만 쓰인 단어 배제 → **2회 이상 등장 단어만** 추천 | `GroupBy(Token)` 후 `if (total < 2) continue;` |

- 비율/추천 분류 선정(`CategoryFrequency`·`RecommendedCategory`) **DTO 로직은 §12-2 그대로 재사용** — 그룹화 키만 `문장 → 단어`로 교체.

**동작 예시 — "파리바게트" 공통 추출**
```
"파리바게트 간식"     → 정제 → "파리바게트 간식"   → ["파리바게트", "간식"]
"파리바게트(미추홀)"  → 정제 → "파리바게트 미추홀"  → ["파리바게트", "미추홀"]   (괄호가 공백으로 치환)

평탄화: (파리바게트→행사비), (간식→행사비), (파리바게트→행사비), (미추홀→행사비)
GroupBy(단어): "파리바게트" = 2건  → ✅ 추천 후보 (TotalCount ≥ 2)
               "간식"·"미추홀" = 각 1건 → ❌ 노이즈 제거로 탈락
```
→ 서로 다른 두 문장에 흩어져 있던 핵심 토큰 `"파리바게트"`가 하나로 모여 추천 후보로 승격.

- **빌드**: 0 Warning / 0 Error.

---

## 13. ✅ 통합 DB 모델 뼈대 구축 (BaseEntity 및 생명주기 엔티티) (2026-06-27)

부서 관리를 단순 가계부에서 **'사역·재정 통합 관리 시스템(ERP)'**으로 확장하기 위한 데이터베이스 뼈대를 신규 구축. 연초 계획부터 분기 마감까지 시간의 흐름(Lifecycle)대로 데이터가 FK로 연결되는 구조를 도입했다.

### 1) BaseEntity 도입 (감사 추적 공통 부모)
- `Models/BaseEntity.cs` (abstract) 신규 생성. 모든 신규 엔티티가 상속.
- 공통 필드: `CreatedAt`(기본값 `DateTime.Now`), `CreatedBy`, `UpdatedAt`, `UpdatedBy`, `IsDeleted`(기본값 false).

### 2) 생명주기 엔티티 5종 신규 생성 (`Models/`)
| 엔티티 | 역할 | 핵심 FK |
|---|---|---|
| `AnnualPlan` | 연간 주차별 행사 계획·목표 예산 (**허브**) | — |
| `WeeklyMeeting` | 주간 회의록 (자유 메모 / 행사 개요) | `AnnualPlanId?` |
| `ExpenseResolution` | 지출 결의서 (사전 기안) | `AnnualPlanId`, `BudgetCategoryId` |
| `Receipt` | 영수증 증빙 | `ExpenseResolutionId` |
| `LedgerTransaction` | 통합 재정 장부 (확정 사실) | `AnnualPlanId?`, `ExpenseResolutionId?` |
- 상태값은 `enum`(`PlanStatus`, `ResolutionStatus`, `LedgerTransactionType`)으로 정의 → EF Core가 int로 저장.
- 기존 `LedgerEntry`(레거시 장부)는 다수 코드 의존으로 **삭제하지 않고 병행 유지**. `LedgerTransaction`은 신규 생명주기의 확정 기록 담당.
- `BudgetCategory` 엔티티는 아직 미구현이므로 `BudgetCategoryId`는 **스칼라 FK 컬럼만** 두고 네비게이션은 보류(향후 정규화 테이블 도입 시 연결).
- `User`(PK가 `string Username`) 참조 Drafter/Approver 관계는 이번 범위에서 제외.

### 3) DbContext 통합 및 마법 로직 (`Data/AppDbContext.cs`)
- 신규 엔티티 5종 `DbSet<T>` 추가. 기존 `IDbContextFactory<AppDbContext>` DI 구조는 그대로 유지(생성자·등록 방식 무변경).
- **소프트 삭제 글로벌 쿼리 필터**: 5종 엔티티에 `HasQueryFilter(e => !e.IsDeleted)` 적용 → 삭제 행 자동 제외.
- **1:1 매핑**: `ExpenseResolution` ↔ `LedgerTransaction` (`HasForeignKey<LedgerTransaction>` + `Restrict`).
- **Cascade 충돌 방지**: 결의서·장부의 `AnnualPlan` 참조에 `Restrict`/`SetNull` 명시, 영수증은 `Cascade`.
- **감사 자동화**: `SaveChanges`/`SaveChangesAsync` 오버라이드 → `Added` 시 `CreatedAt`, `Modified` 시 `UpdatedAt` 자동 갱신(수정 시 `CreatedAt`/`CreatedBy`는 보존).

### 4) 검증 & 후속
- **빌드: 0 Warning / 0 Error.**
- ⚠️ 마이그레이션은 미적용 상태. 사용자가 직접 실행 예정(아래 5)항의 **스키마 적용 주의사항** 참고).

### 5) 3가지 보류 이슈(User 참조, 기존 Category 연결, 장부 마이그레이션 헬퍼) 반영 완료 (2026-06-27)
- **User(Drafter/Approver) 명시 연결**: `ExpenseResolution`에 `string? DrafterId`/`ApproverId` + 네비게이션 추가. 기존 `User`의 PK가 `string Username`이라 FK도 `string?`으로 일치시킴. `OnModelCreating`에서 두 FK 모두 `OnDelete(Restrict)`(동일 테이블 다중 FK Cascade 충돌 방지).
- **기존 예산 모델 재사용(중복 방지)**: 임시 `int BudgetCategoryId` → 기존 `BudgetPlan`(PK `int Id`)을 참조하는 `int? BudgetPlanId` + 네비게이션으로 교체(`ExpenseResolution`·`LedgerTransaction` 모두). `CategoryMapping`은 키워드 자동분류 규칙이라 대상이 아님. 참조 중 `BudgetPlan` 삭제는 `Restrict`로 차단(이력 보존).
- **구/신 장부 마이그레이션 헬퍼**: `AccountingService.MigrateLedgerDataAsync()` 작성. 레거시 `LedgerEntry`(`Income`/`Expense` 분리 컬럼) → 신규 `LedgerTransaction`(`Type` enum + 단일 `Amount`)로 변환. 멱등성(소프트삭제분 포함 `IgnoreQueryFilters`), 이상치 격리(양쪽 0/양쪽 입력/음수), `BudgetPlan` 정규화 매칭(Trim+대소문자 무시), `LedgerMigrationResult` 리포트 반환.
- **적대적 다중 에이전트 리뷰(20 agents) 통해 발견·수정**: 멱등성 가드 필터 무력화, 금액 매핑 엣지케이스, FiscalYear 매칭 정규화, 반환값 모호성, 유니크 인덱스 주석 불일치 등 확정 결함 반영.
- ✅ **빌드 0/0**, `dotnet ef dbcontext info` 설계시점 모델 검증 통과.

> ⚠️ **스키마 적용 주의(중요)**: 현재 `DbInitializer`는 `EnsureCreated()`를 사용하며 **기존 `church.db`에는 신규 5개 테이블을 추가하지 못한다**(라이브 DB에 신규 테이블·`__EFMigrationsHistory` 모두 없음 확인). 따라서 `migrations add` 후 단순 `database update`는 기존 DB에서 *"table already exists"* 로 실패한다. → **기존 마이그레이션 6건을 `__EFMigrationsHistory`에 베이스라인 등록 후** 신규 마이그레이션만 적용해야 하며, 장기적으로 시작 시점 `EnsureCreated()` → `Database.Migrate()` 전환을 권장. (헬퍼는 테이블 부재 시 예외 대신 `SchemaReady=false`로 안전 종료하도록 방어 처리됨.)

### 6) 표준화 및 마이그레이션 최종 단계 완료 (2026-06-27)
- **시스템 초기화 `Database.Migrate()` 전환 완료**: `DbInitializer.Initialize()`의 `EnsureCreated()` → `context.Database.Migrate()`로 교체. 신규 환경은 마이그레이션 전체 적용으로 스키마+이력을 일관 생성, 기존 환경은 보류분만 적용. (Program.cs는 `AddDbContextFactory` 등록이라 `GetRequiredService<AppDbContext>()` 대신 기존 `IDbContextFactory.CreateDbContext()` 패턴 유지가 정답 — 검증 완료.)
- **최고관리자용 장부 이관 UI 연결 및 헬퍼 호출부 생성 완료**: `Settings.razor` [데이터 및 엑셀 관리] → [월별 장부 백업] 서브탭의 **최고관리자(`IsSystemAdmin`) 전용** 영역에 "기존 장부 데이터 이관" 버튼 신설. 클릭 시 `AccService.MigrateLedgerDataAsync()` 호출, 결과(`LedgerMigrationResult`: 이관/예산연결/이상치격리/이미이관/스키마미적용)를 배지·메시지로 표시. 핸들러에 권한 이중 가드 포함.
- **운영 배포 시 `__EFMigrationsHistory` 베이스라인 생성 절차 확정**:
  1. `dotnet ef migrations add LinkLifecycleEntities` (신규 5개 테이블만 추가하는 마이그레이션 생성)
  2. 기존 `church.db`에 `__EFMigrationsHistory` 생성 후 레거시 6건(`20260322113210_AddDepartmentBankInfo` ~ `20260325113804_Add2FA`, ProductVersion `8.0.0`)을 `INSERT OR IGNORE`로 베이스라인 등록 (신규 ID는 제외)
  3. `dotnet ef database update` → 신규 마이그레이션만 실제 적용
  4. 이후 앱 기동 시 `Migrate()`는 적용 완료 상태를 확인하고 무동작(no-op). ⚠️ **베이스라인 전 앱 기동 금지**(기동 시 레거시 테이블 충돌).
- ✅ **빌드 0 Warning / 0 Error.**

---

## 14. ✅ 생명주기 화면 구축 + 예산 분류 체계 재정비 + 운영 배포 (2026-07~08)

§13에서 만든 DB 뼈대 위에 실제 화면을 올리고, 예산·장부의 분류 체계를 표준에 맞게 재정비한 뒤 운영 서버에 배포했다. 상세 설계 배경은 `생명주기_설계메모.md`, 분류 체계 근거·결과는 `예산_분류체계_개선안.md` 참조.

### 1) 목적 재정의 — "결재 통제"가 아니라 "기록의 인수인계"
사용자 인터뷰로 시스템의 진짜 목적이 확정되었다. 교육국 임원이 **2년마다 교체**되므로, 다음 담당자가 "작년 이 행사를 언제·얼마로 치렀나"를 파악해 올해 계획을 세우는 것이 핵심이다. 따라서 **결재 워크플로우(작성→승인→반려)는 만들지 않기로** 했고, **행사 사후 보고서가 최종 산출물**로 규정되었다.

### 2) 마이그레이션 베이스라인 등록 및 EF 도구 정합 (2026-07-10)
- §13-6에서 확정한 절차대로 운영 `church.db`에 `__EFMigrationsHistory` 생성 후 **레거시 6건 베이스라인 등록** 완료. 등록 전 앱 기동 금지 원칙을 지켜 충돌 없이 처리.
- Codespace의 `dotnet-ef`가 **10.0.3**인데 프로젝트는 EF Core **8.0.0**이라 마이그레이션 생성 시 스냅샷이 EF10 형식으로 덮일 위험이 있었다. 로컬 도구 매니페스트(`.config/dotnet-tools.json`)를 **8.0.0으로 고정**해 해결.
- `LinkLifecycleEntities` 마이그레이션 생성. `Up`은 신규 5개 테이블 `CreateTable`만, `DropTable`은 `Down`에만 존재함을 확인 후 진행.

### 3) 자동 배포 일시 중단 → 수동 배포 전환
UI 확인 없이 운영에 나가는 것을 막기 위해 `deploy.yml`의 `push` 트리거를 주석 처리하고 **`workflow_dispatch`(수동 실행)** 로 전환. 개발 중에는 푸시해도 배포되지 않고, 준비되면 Actions 탭에서 버튼으로 배포한다.

### 4) 연간 계획 화면 (`/annual-plan`)
- **카드 그리드** + 분기(1~4) · 상태(계획/진행/완료/마감) **필터 칩**, 요약 카드(건수·목표수입·편성지출)
- **날짜 → 회계연도·분기·주차 자동 계산**: 부서별 분기 설정을 따르는 기존 `CalculateFiscalYearAsync`/`GetQuarterNumberAsync`를 그대로 재사용(임의 계산 금지). 주차는 **일요일 시작** 기준으로 월주차 표시 + 연중주차(`WeekNo`) 저장.
- **행사 기간**: `AnnualPlan.EndDate`(nullable) 추가. 시작일 변경 시 종료일이 다음날로 자동 추종, 시작일 기준으로 회계연도·분기 계산.
- **엑셀/CSV 일괄 등록**: 양식 다운로드 → 업로드 → 미리보기(분기 자동계산) → 일괄 저장. 기존 `downloadExcelFile` JS와 ClosedXML 재사용.
- **행사명 datalist**: 예산 카테고리를 자동완성으로 제안(`GetCategorySuggestionsAsync` 재사용). 예산과 이름이 어긋나 보고서 매칭이 깨지는 것을 예방하면서 신규 행사 직접입력도 허용.
- 신규 필드: `AnnualPlan.DepartmentId`, `EventDate`, `EndDate` (마이그레이션 3건)

### 5) 주간 회의록 화면 (`/weekly-meeting`)
- **회의 날짜별 그룹** + 그 아래 기록 카드. 한 회의에서 여러 행사를 논의하면 **행사별로 기록이 분리**된다(설계 의도와 일치).
- 행사 연결 기록(골드 띠)과 일반 안건(회색 띠) 구분. 행사 선택 시에만 일시·장소·예상인원 입력란 노출.
- **행사 필터**로 "여름성경학교 회의록만" 모아 보면 준비 과정이 시간순으로 추적된다.
- `WeeklyMeeting.DepartmentId` 추가(부서별 분리 필수). 회계연도 범위는 분기 설정(q1.Start~q4.End)으로 산출.

### 6) 예산 분류 체계 재정비 — 행사(WHY) / 비용(WHAT) 2축 분리
**문제**: `Category`에 행사("여름성경학교")와 비용분류("행사비")가 뒤섞여 있었고, "행사비" 아래에 개별 행사(생일축하·성탄절·졸업선물…)가 들어가 **위계가 뒤집혀** 있었다. 이 상태로는 개별 행사의 지출을 집계할 수 없다.

**해결**: 비영리 회계 표준(UCOA/GAAP, QuickBooks 클래스 개념)에 따라 축을 분리했다.
```
Category    = 행사 (WHY)   예: 여름성경학교, 달란트, 성탄절
SubCategory = 비용 (WHAT)  예: 식사, 선물비, 진행비
```
- "행사비" 해체 → 개별 행사를 독립 Category로 승격, Category명의 "행사" 글자 제거
- 행사가 아닌 상시 지출(훈련비·공과비·교사회의비·부서관리비·전도비) → **"운영비"로 통합**
- 성탄절선물 + 성탄절준비비 → "성탄절" 하나로 묶고 Sub로 구분
- 예산 외 집행된 행사(어린이날·예능대회·기타행사)는 **0원 예산으로 추가**해 집행 사실을 드러냄

**기존 거래 재분류의 제약**: 장부 설명이 대부분 카드 가맹점명("지에스더프레시" 등)이라 자동 판별이 불가능했다. → 날짜·메모 기반 **추정값을 채운 검토용 엑셀**을 만들어 사용자가 확정하는 방식으로 처리(지출 78건, 수입 73건).

### 7) `Transactions.SubCategory` 추가 — 수입의 행사 연결
정산 보고서가 행사별 **수입**도 자동 집계해야 하는데, 거래 테이블에 세부 분류 필드가 없었다.
- `LedgerEntry`에 `string? SubCategory` 추가 (마이그레이션 `AddTransactionSubCategory`)
- **수입은 지출과 다르게 처리**: `Category`(회비수입/찬조금)를 **유지**해 회계 총계를 보존하고, `SubCategory`에 행사명을 넣어 연결. → 회비수입 1,006,400 / 찬조금 550,000 총계 불변.
- `MonthlyLedger`의 편집 버퍼에도 `SubCategory`를 포함해 수정 시 값이 유실되지 않게 처리.

### 8) 운영 배포 및 데이터 적용 (2026-08-02)
- 커밋 6건 푸시 후 Actions에서 **수동 배포**. 앱 기동 시 `Migrate()`가 신규 5건을 자동 적용 → 운영 마이그레이션 **11건**(레거시 6 + 신규 5) 완료.
- 앱 중지 → 백업 → 재분류 SQL 실행 → 재시작 순으로 유년부 2026 데이터 적용.
- **결과**: 예산 합계 14,039,000 불변, "행사비" 0건, 지출이 11개 행사로 분리, 수입 73건이 행사에 연결.

### 9) ⚠️ 이번에 얻은 교훈 (재발 방지)
- **SQLite 트랜잭션**: `sqlite3 db < script.sql` 실행 시 `BEGIN TRANSACTION;`만 있고 `COMMIT;`이 없으면 **조용히 롤백**된다. 그런데 스크립트 내 검증 쿼리는 트랜잭션 안에서 실행되어 **성공한 것처럼 보인다** → 실제로 두 번 속았다. 또한 `echo "COMMIT;" >> file`은 줄바꿈이 없어 앞 주석 줄에 붙어 무효화되므로 `printf '\nCOMMIT;\n'`을 써야 한다. **철칙: 실행 후 별도 세션으로 재조회해 검증할 것.**
- **Razor 파싱**: `@표현식` 뒤에 한글이 공백 없이 붙으면 한글까지 C# 식별자로 삼켜 컴파일 오류(`@_dateInfo.Month월` → `Month월` 멤버로 해석). `@(...)`로 감쌀 것.
- **Blazor 모달**: 배경 div에 `@onclick="닫기"`를 걸면 입력란 클릭 이벤트가 전파되어 **모달이 저절로 닫힌다**(특히 `type="number"`). 배경 클릭 닫기를 쓰지 않는 편이 안전.
- **fire-and-forget 금지**: `_ = SomeAsync();`로 async를 던지면 예외가 삼켜지고 렌더 타이밍이 깨져 컴포넌트가 죽는다. 핸들러를 `async Task`로 만들고 `await`할 것.

### 10) 남은 과제
- **행사 사후 보고서 화면** (다음 작업) — 행사 선택 시 장부에서 지출·수입 자동 집계, 예산 대비 비교, 잘된점·개선점·담당자·사진 기록
- **월별장부 입력 UI에 SubCategory 입력란 추가** — 재분류는 했으나 신규 입력 시 여전히 Category만 들어가므로 시간이 지나면 다시 어긋난다(우선순위 높음)
- 영유아부 등 **다른 부서 분류 재정비** (부서마다 위계가 다르게 뒤집혀 있음)
- 연도 간 비교(YoY), 담당자 필드, '취소' 상태값 검토

---

## 15. ✅ 장부 2축 입력 + 행사 사후보고서 — 생명주기 완성 (2026-08-03)

§14에서 예산·장부의 분류 체계를 행사(WHY)/비용(WHAT) 2축으로 재정비했다. 이번에는 **앞으로 입력될 데이터도 그 체계를 따르도록** 장부 입력 UI를 고치고, 이 시스템의 최종 산출물인 **행사 사후보고서**를 만들어 생명주기를 완성했다.

### 1) 월별장부에 세부(SubCategory) 입력란 추가
**배경**: §14에서 기존 데이터는 재분류했지만, 장부 입력 화면은 여전히 `Category`만 받고 있었다. 그대로 두면 새로 입력되는 거래마다 행사·비용 구분이 빠져 **시간이 지날수록 다시 어긋난다**. 재분류 직후에 막는 것이 우선순위였다.

- 테이블에 **"세부" 컬럼 추가** — 표시행·신규입력행·편집행 세 곳 모두 대응
- **분류에 연동된 자동완성**: 단일 목록이 아니라 `Dictionary<string, List<string>>` 맵으로 구성해, 선택한 분류에 편성된 세부만 제안한다.
  - 겨울성경학교 → 식사, 예비비, 프로그램준비비
  - 성탄절 → 선물비, 준비비
  - 운영비 → 공과교재, 교사간식, 교사회식, 교사훈련(MT), 물품구입, 새친구선물, 주간공과, 환경미화
- **수입/지출 구분**: 지출은 비용 종류를, 수입은 행사명을 제안한다(수입의 SubCategory는 "어느 행사의 회비인가"를 뜻하므로).
- 예산에 없는 값도 장부에 쓰였다면 후보에 포함하고, 직접 입력도 막지 않는다.
- 컬럼 너비 재조정: 분류항목 10%→8%, 세부 7%, 내역 20%→**25%**(가맹점명이 길어 가장 넓어야 함)

### 2) 행사 사후보고서 (`/event-report`) — 최종 산출물
2년마다 바뀌는 임원에게 "작년 이 행사를 언제·누가·어디서·어떻게, 돈은 어떻게 흘렀나"를 넘겨주는 화면. §14에서 정의한 시스템의 목적이 여기서 결실을 맺는다.

**핵심 설계 — 재정 수치를 저장하지 않는다**
보고서 엔티티(`EventReport`)에는 금액을 담지 않고, 조회할 때마다 예산·장부에서 행사명으로 매칭해 집계한다. 나중에 거래를 수정해도 보고서가 자동으로 따라가므로 데이터가 어긋날 여지가 없다.
```
지출 = Transactions.Category    = 행사명
수입 = Transactions.SubCategory = 행사명
예산 = BudgetPlans.Category     = 행사명
```

**목록 화면**: 집행 내역이 있는 모든 행사가 자동으로 뜬다. 보고서를 쓴 것은 "작성완료", 안 쓴 것은 점선 카드로 "미작성" → **빠진 게 한눈에 보인다.** 상단에 진행률(예: 3/11)과 집행·수입 총액. 각 카드에 예산·지출·수입이 자동 집계되고 초과 시 경고.

**작성 페이지**: 위쪽은 자동 집계 재정 요약(편성예산·실제지출·수입·순비용 + **세부별 비용 내역 칩**), 아래쪽은 직접 입력(기간·장소·인원·**담당자**·진행내용·**잘된 점**·**개선할 점**). §14에서 맹점으로 짚었던 "누가 맡았나"를 담당자 필드로 해결했고, 개선할 점이 다음 담당자에게 전달되는 인수인계의 핵심이다.

**인쇄 지원**: `@@media print`로 입력란 테두리를 없애고 문서 형태로 출력. 배포·보관용.

**신규 엔티티**: `EventReport : BaseEntity` (마이그레이션 `AddEventReport`)
⚠️ 기존 `ExpenseReport`(결재·입금용 지출보고서)와 **다른 것**이다. `AnnualPlan`과는 선택적 FK(`SetNull`)로 연결해, 연간계획이 지워져도 보고서 기록은 남는다.

### 3) ⚠️ 이번에 얻은 교훈
- **`colgroup`의 `<col>` 개수는 열 개수와 반드시 일치해야 한다.** 세부 컬럼을 추가하면서 `<th>`만 늘리고 `<col>`을 빠뜨려 너비가 뒤로 한 칸씩 밀렸다. `colspan`(합계 행)도 함께 조정해야 한다(6→7, 5→6).
- **도커 배포 시 컨테이너가 교체되지 않을 수 있다.** 파일은 서버에 전송됐는데(DLL 타임스탬프 최신) 컨테이너는 6시간 전 것이 계속 돌아 새 화면이 안 나왔다. `docker-compose up -d`만으로는 이미지 변화가 없다고 판단되면 재생성하지 않는다. → `deploy.yml`에 **`--force-recreate`** 추가로 해결.
  진단법: `docker ps`의 `CREATED AT`과 `ls -la *.dll`의 시각을 비교한다.

### 4) 현재 상태 — 생명주기 완성
```
연간 계획(/annual-plan) → 주간 회의록(/weekly-meeting)
   → 장부 입력(행사·비용 2축) → 행사 보고서(/event-report)
```
유년부 2026 기준으로 "여름성경학교: 예산 2,548,000 / 지출 2,937,790 / 수입 697,000 → 예산 초과 389,790"이 자동 산출된다. 다음 담당자가 이 숫자와 개선 메모를 보고 내년 예산을 편성할 수 있다.

### 5) 남은 과제
- **영유아부 등 다른 부서 분류 재정비** (부서마다 위계가 다르게 뒤집혀 있음 — 영유아부는 `성경학교 > 겨울성경학교` 형태)
- 보고서 **사진 첨부** (이번엔 글만)
- **연도 간 비교(YoY)** — "작년 여름성경학교는 이랬다"를 나란히 보기
- 연간계획 카드에 목표수입 표시 등 미뤄둔 UI 다듬기
- 비밀번호 평문 저장 → 해시 저장으로 개선 (별도 작업 필요)

---

## 16. ✅ 연도 비교·양식 개선·화면 통일·비밀번호 해시화 (2026-08-03)

생명주기 4단계를 완성한 뒤, 실사용에서 드러난 개선점과 미뤄둔 보안 과제를 처리했다.

### 1) 연간계획에 "지난 실적" 표시 — 계획하는 순간에 작년을 본다
이 시스템의 목적이 "다음 담당자가 작년을 보고 올해를 계획하는 것"인데, 정작 계획 화면에서는 한 해씩만 볼 수 있었다.

- 연간계획 모달에서 **행사명을 입력·선택하면** 직전 3개년 실적(편성·집행·수입·편차)이 **금액 입력란 바로 위에** 자동으로 뜬다.
- 예산을 초과한 해는 붉게 표시하고 "편성액 결정 시 참고하세요" 안내를 덧붙인다.
- 실적이 없으면 "신규 행사"로 표시.
- 기존 `GetEventFinancesAsync`를 연도만 바꿔 재호출하므로 **서비스·엔티티 변경이 없다**(razor 한 파일만 수정).

**한계**: 2025년은 재분류가 안 되어 있어 "행사비"에 묻힌 행사(달란트·성탄절 등)는 비교되지 않는다. 성경학교처럼 독립 카테고리였던 행사만 대조된다. 2025를 재분류하면 자동으로 더 많이 보이게 된다.

**실제 사례**: 겨울성경학교 2025년 편성 1,360,000 / 집행 2,164,635(80만 초과) → 2026년엔 1,408,000으로 상향 편성. 이 판단을 화면에서 바로 할 수 있게 됐다.

### 2) 일괄등록 양식 개편 — 빈 양식에서 "작년 계획 불러오기"로
매년 비슷한 행사가 반복되므로, 빈 칸을 채우는 것보다 **작년 것을 고쳐 쓰는 편**이 실무에 맞다.

- 양식을 받으면 **직전 연도 계획이 그대로 채워져** 있다(행사명·추진내용·금액).
- **날짜는 1년 뒤로 자동 조정**되어 있어 실제 일정에 맞게 고치기만 하면 된다.
- 오른쪽에 **작년 편성·집행·편차**가 참고용(회색)으로 함께 나오고, 초과 행사는 붉게 표시된다. 이 열은 파싱 대상이 아니라(앞 7열만 읽음) 그대로 업로드해도 무방하다.
- 파일명도 `연간계획_2027_유년부.xlsx`처럼 구체화.

**교훈 — ClosedXML 타입**: `Cell().Value`는 `XLCellValue`라 `object`를 못 받는다. `(object?)null`을 쓰는 삼항 연산자는 CS0266 컴파일 오류를 낸다. 값이 있을 때만 `if`로 분기해 넣을 것.
**교훈 — 열 너비**: `AdjustToContents()`는 한글·날짜 폭을 제대로 계산하지 못하고(날짜가 `###`으로 표시), 앞서 설정한 개별 열 너비까지 덮어쓴다. 열 너비는 직접 지정하고 날짜 서식(`yyyy-mm-dd`)도 명시할 것.

### 3) 화면 스타일 통일
생명주기 화면 3개(연간계획·주간회의록·행사보고서)가 기존 페이지와 톤이 달랐다. 제목 옆 아이콘 박스, `max-width` 중앙 정렬, 작은 설명문 폰트가 원인.

기존 페이지(`DataCollectionHub`·`ExpenseReportQuarterly`) 규격으로 통일:
- 아이콘 박스 제거, `max-width` 제거(화면 폭 사용)
- 제목 `clamp(1.5rem, 3vw, 2rem)` / weight 700 / Manrope
- 설명문 `1.2rem` + 화면 역할 설명 추가("한 해 행사를 미리 계획합니다" 등)

### 4) 매뉴얼에 생명주기 안내 추가
도움말(`Manual.razor`)에 생명주기 기능 설명이 전혀 없었다. 기존 단계 카드 디자인을 그대로 따라 **5단계 "사역 기록 남기기 — 다음 담당자를 위해"** 를 추가했다. 흐름도(연간계획→주간회의록→월별장부→행사보고서), 각 화면의 역할, 일괄등록 활용법, "왜 기록을 남기나"를 담았다.

### 5) 🔐 비밀번호 해시 저장 (평문 → PBKDF2)
DB만 열면 전 계정 비밀번호가 보이던 문제를 해결했다.

**방식**: PBKDF2/HMAC-SHA256, 10만 회 반복, 16바이트 salt. 저장 형식 `pbkdf2$반복$salt$hash`.
외부 패키지 없이 .NET 내장 암호화만 사용(배포 위험 최소화). 신규 파일 `Services/PasswordHasher.cs`.

**핵심 설계 — 자동 이행(무중단)**
일괄 변환은 **의도적으로 피했다.** 이미 비밀번호를 바꾼 계정(유년부 등)이 있어서, `UPDATE ... = Hash('1234')`를 돌리면 그 계정이 로그인 불가가 된다.

대신 로그인 시점에 점진 전환한다:
1. 저장값이 해시면 → 해시 검증
2. 평문이면 → 평문 비교 후 **성공 시 그 비밀번호를 해시로 교체 저장**
3. 각자 한 번 로그인하면 자연히 전환

이 방식은 **누가 어떤 비밀번호를 쓰든 안전**하며, 마이그레이션 SQL도 필요 없다.

**변경 지점 6곳**: `Login.razor`(검증+이행), `AccountingService`(변경·초기화), `DbInitializer`(기본계정 7개), `Settings.razor`(신규계정 시드).

**제거된 기능**: "현재 비밀번호 보기". 해시는 복원이 불가능하므로 원리적으로 불가하다. 화면은 "암호화되어 저장되므로 표시되지 않습니다 / 잊으셨다면 관리자에게 초기화를 요청하세요" 안내로 교체했다. 사용자 확인 결과 실제로 쓰지 않던 기능이었다.

**남은 일**: 오래 로그인하지 않는 계정은 평문으로 남는다. 시간이 지난 뒤 아래로 확인하고, 필요하면 개별 초기화한다.
```bash
sqlite3 church.db "SELECT Username, CASE WHEN Password LIKE 'pbkdf2$%' THEN '해시' ELSE '평문' END FROM Users;"
```

### 6) 남은 과제
- **영유아부 등 다른 부서 분류 재정비** — 검토표까지 만들어둔 상태(사용자 검토 대기). 유년부와 위계가 다르게 뒤집혀 있음(`성경학교 > 겨울성경학교`, `행사비 > 12개 행사`)
- **2025 회계연도 재분류** — 하면 연도 비교가 훨씬 풍부해짐
- 보고서 **사진 첨부**, 연간계획 카드에 목표수입 표시 등
- 부서별 결산 합계 검증 화면 (실제로 부서가 잘못 입력된 사례가 있었음)

---

## 부록 — 별건 처리 이력 (대시보드 보조금 비율)

1단계 이전, 대시보드 "보조금 수령 비율" 계산 오류를 별도로 수정.
- 분자를 **실수령 거래액** → **지출결의서 신청액 누계**(`reports.Sum(r => r.TotalAmount)`)로 변경
- 강제 100% 컷 제거, 카드 표시를 `신청액 / 예산` 기준으로 정렬
- 배지 문구 `보조금 수령` → `보조금 신청`, 미사용 `SubsidyReceived` 필드 제거
