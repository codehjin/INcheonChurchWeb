# 사역·재정 생명주기 설계 메모

> **목적**: 연간계획 → 주간회의록 → 장부 → 행사 사후보고서로 이어지는 기능의
> 설계 결정·맹점·로드맵 기록. 다른 세션/Codespace Claude가 이어받을 때 맥락 유지용.
> **최종 갱신**: 2026-08-03 (생명주기 4단계 완성 + 운영 배포 완료)

---

## 1. 이 시스템의 진짜 목적 (가장 중요)

**결재 프로세스 통제가 아니라, 기록의 인수인계다.**

- 교육국 임원은 **2년마다 교체**된다. 다음 담당자가 "작년 이맘때 이 행사를
  언제·얼마로·어떻게 치렀나"를 한눈에 보고 올해 계획을 세우는 것이 핵심 목표.
- 강제 워크플로우(작성→승인→반려)는 **불필요**. 사용자가 명시적으로 확인함.
- **최종 산출물 = 행사 사후 보고서.** "언제·누가·어디서·어떻게, 돈의 흐름"으로
  마무리되어야 다음 해 집행계획의 참고가 된다.

---

## 2. 핵심 구조: "행사 = 예산 카테고리"

```
"여름성경학교" (문자열 하나가 전체를 관통하는 열쇠)
   ├── 예산(BudgetPlan.Category)          : 편성액 2,548,000
   ├── 장부 지출(Transactions.Category)    : 실제 2,937,790
   ├── 장부 수입(Transactions.SubCategory) : 회비·찬조 697,000
   ├── 연간계획(AnnualPlan.Title)          : 날짜·상태
   └── 행사보고서(EventReport.EventName)   : 위를 자동 집계 + 정성 기록
```

**2축 분리 원칙** (비영리 회계 표준):
- 지출: `Category` = 행사(WHY), `SubCategory` = 비용 종류(WHAT)
- 수입: `Category` = 회비수입/찬조금(총계 보존), `SubCategory` = 행사명

---

## 3. 확정된 설계 결정

| 항목 | 결정 |
|---|---|
| 결재 상태(승인/반려) | **불필요.** 만들지 않음 |
| 행사 입력 정보 | 제목·예상 날짜·예상 금액이면 충분 |
| 주간회의록 성격 | 준비 과정 협의의 **리마인드 용도** (행사별 분리 기록) |
| 사후 보고서 위치 | **별도 화면** 목록 + 전체 페이지 작성 |
| 보고서의 재정 수치 | **저장하지 않음.** 조회 시마다 장부에서 집계 |
| 보고서 상세도 | 기간·장소·인원·담당자 + 진행내용·잘된점·개선점 (사진은 추후) |
| 인쇄/PDF | `@@media print` 지원 |
| 연간계획 행사명 | 예산 카테고리 **datalist 자동완성** + 직접입력 병행 |
| 장부 세부 입력 | 분류에 연동된 자동완성 (분류별 맵) |
| 주차 계산 | 일요일 시작. 월주차 표시 + 연중주차 저장 |
| 분기/회계연도 | 부서별 설정 연동(`AccountingService` async 메서드 재사용) |

---

## 4. 맹점 (Blind Spots)

1. ~~계획 대비 실제(편차)~~ → ✅ 보고서에서 예산·지출·수입 자동 대조
2. ~~"누가"의 기록~~ → ✅ 보고서에 담당자 필드 추가
3. ~~월별장부 입력 UI에 SubCategory 없음~~ → ✅ 해결
4. **연도 간 비교(YoY)** — "작년 여름성경학교는 이랬다"를 나란히 보기. **미구현**
5. **미완료·취소 행사** — 상태값에 '취소' 추가 검토. **미구현**
6. **다른 부서 분류 재정비** — 영유아부 등은 위계가 다르게 뒤집혀 있음. **미착수**
7. ~~비밀번호 평문 저장~~ → ✅ PBKDF2 해시 저장 + 자동 이행 방식으로 해결

---

## 5. 현재 구현 상태 (2026-08-03 · 전부 운영 배포됨)

| 기능 | 라우트 | 상태 |
|---|---|---|
| 연간 계획 | `/annual-plan` | ✅ |
| 주간 회의록 | `/weekly-meeting` | ✅ |
| 장부 2축 입력 | `/monthly` | ✅ |
| 행사 사후보고서 | `/event-report` | ✅ |

**추가 완료 (2026-08-03)**
- 연간계획에 **지난 3개년 실적** 표시 (계획 시 작년 참고)
- 일괄등록 양식이 **작년 계획을 채워서** 다운로드 (날짜 1년 자동 조정 + 작년 실적 참고 열)
- 화면 스타일 통일 (아이콘 제거, 폭·폰트를 기존 페이지 규격으로)
- 매뉴얼에 생명주기 5단계 안내 추가
- **비밀번호 PBKDF2 해시 저장** (자동 이행 방식, `Services/PasswordHasher.cs`)

**연간 계획**: 카드 그리드, 분기+상태 필터, 날짜→회계연도·분기·주차 자동계산,
행사기간(시작~종료), 엑셀/CSV 일괄등록, 행사명 datalist 자동완성

**주간 회의록**: 회의 날짜별 그룹, 행사 연결/일반안건 구분, 행사 필터

**장부**: 분류(행사) + 세부(비용) 2축 입력. 분류에 연동된 자동완성

**행사 보고서**: 목록(미작성 표시·진행률) + 작성 페이지(자동 재정 집계 + 정성 기록) + 인쇄

### 엔티티 현황
- `AnnualPlan`: DepartmentId, EventDate, EndDate, FiscalYear, WeekNo, Quarter,
  Title(행사명), Description, PlannedIncome, PlannedExpense, Status
- `WeeklyMeeting`: DepartmentId, MeetingDate, FreeMemo, EventDateTime,
  EventLocation, ExpectedAttendees, AnnualPlanId
- `EventReport`: DepartmentId, FiscalYear, **EventName**(집계 열쇠), AnnualPlanId(선택),
  StartDate, EndDate, Location, Attendees, Organizer, Content, WhatWentWell, WhatToImprove
  → **재정 수치는 없음** (장부에서 매번 집계)
- `LedgerEntry`(Transactions): …, **Category**(행사), **SubCategory**(수입:행사/지출:비용), …
- `BudgetPlan`: Year, DepartmentId, Type, **Category**(행사), SubCategory(비용), Amount

**⚠️ 이름 혼동 주의**: `ExpenseReport`는 기존 **결재/입금용 지출보고서**.
`EventReport`(행사 사후보고서)와 **다른 엔티티**다.

### 배포 상태
- **자동 배포 꺼짐** (`deploy.yml` → `workflow_dispatch`). Actions에서 수동 실행.
- `deploy.yml`에 **`--force-recreate`** 적용됨 (컨테이너 미교체 문제 해결)
- 운영 마이그레이션 12건 적용 (레거시 6 + 신규 6)
- `DbInitializer.Initialize()`의 `Database.Migrate()`가 앱 기동 시 자동 적용
  → **별도 `dotnet ef database update` 불필요**. 베이스라인 등록 완료로 충돌 없음
- 유년부 2026 재분류 운영 적용 완료 (`예산_분류체계_개선안.md` 참조)

---

## 6. 다음 작업 후보

1. **영유아부 등 다른 부서 분류 재정비** — 검토표까지 작성 완료, 사용자 검토 대기.
   영유아부는 위계가 또 다르게 뒤집혀 있음(`성경학교 > 겨울성경학교`, `행사비 > 12개 행사`)
2. **2025 회계연도 재분류** — 하면 연도 비교가 훨씬 풍부해짐
3. **보고서 사진 첨부** — 기존 영수증 업로드 인프라 재사용 가능
4. **UI 다듬기** — 연간계획 카드에 목표수입 표시, 상태 색 구분 등
5. **부서별 결산 검증 화면** — 실제로 부서가 잘못 입력된 사례가 있었음
6. **평문 잔여 계정 정리** — 오래 로그인하지 않는 계정은 평문으로 남음. 주기적 확인 필요

---

## 7. 작업 안전 수칙 (그동안 겪은 함정)

### Codespace 협업
- 대형 파일(`AccountingService.cs`)은 **덮어쓰지 말고 블록만 추가**
- 신규 razor는 드래그로 직접 배치, 엔티티/서비스는 프롬프트로 "추가"만
- 마이그레이션 생성 후 `Up`이 **AddColumn/CreateTable만** 하는지 확인
- 커밋 시 `church.db*`·`publish_output/` 제외. 푸시는 명시 지시 후에만
- Codespace를 새로 만들면 **`git pull` 먼저** (파일 목록이 GitHub와 다를 수 있음)

### Blazor/Razor 함정
- `@표현식` 뒤 한글은 `@(...)`로 감싼다 (한글도 C# 식별자라 파싱 오류)
- CSS의 `@media`는 razor에서 **`@@media`**로 이스케이프
- async는 `_ = ...`(fire-and-forget) **금지**, `await` 사용
- `@bind:after` 사용 시 `StateHasChanged()` 중복 호출 금지
- **모달 배경에 `@onclick="닫기"` 금지** — 입력란 클릭 시 전파로 모달이 닫힘
- **테이블 열 추가 시**: `<th>`, `<td>`, **`<col>`**, **`colspan`** 넷 다 맞춰야 함

### ClosedXML (엑셀)
- `Cell().Value`는 `XLCellValue`라 **`object`를 못 받는다**. `(object?)null` 삼항 연산자는
  CS0266 오류 → 값이 있을 때만 `if`로 분기해 넣을 것
- `AdjustToContents()`는 한글·날짜 폭을 못 잡고(날짜 `###`) 개별 설정도 덮어쓴다
  → 열 너비 직접 지정 + 날짜 서식(`yyyy-mm-dd`) 명시

### DB·배포
- 운영 DB 수정 전 **반드시 백업**
- SQL 실행 시 앱 중지(`docker-compose stop`) → 실행 → 재시작
- **트랜잭션 스크립트는 `COMMIT;` 필수**, 실행 후 **별도 세션으로 재조회 검증**
- **배포 후 컨테이너 교체 확인**: `docker ps`의 CREATED AT vs `ls -la *.dll` 시각 비교
