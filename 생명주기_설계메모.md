# 사역·재정 생명주기 설계 메모

> **목적**: 연간계획 → 주간회의록 → (지출) → 행사 사후보고서로 이어지는 기능의
> 설계 결정·맹점·로드맵 기록. 다른 세션/Codespace Claude가 이어받을 때 맥락 유지용.
> **최종 갱신**: 2026-08-02 (운영 배포 + 유년부 재분류 완료 시점)

---

## 1. 이 시스템의 진짜 목적 (가장 중요)

**결재 프로세스 통제가 아니라, 기록의 인수인계다.**

- 교육국 임원은 **2년마다 교체**된다. 다음 담당자가 "작년 이맘때 이 행사를
  언제·얼마로·어떻게 치렀나"를 한눈에 보고 올해 계획을 세우는 것이 핵심 목표.
- 강제 워크플로우(작성→승인→반려)는 **불필요**. 사용자가 명시적으로 확인함.
- 지출결의서 내역은 대개 미확정·형식적. **지출 완료 후 영수증이 모여야**
  개별 행사의 사후 보고서를 쓸 수 있다.
- **최종 산출물 = 행사 사후 보고서.** "언제·누가·어디서·어떻게, 돈의 흐름"으로
  마무리되어야 다음 해 집행계획의 참고가 된다.

---

## 2. 핵심 구조: "행사 = 예산 카테고리"

```
"여름성경학교" (문자열 하나가 전체를 관통하는 열쇠)
   ├── 예산(BudgetPlan.Category)    : 편성액 2,548,000
   ├── 장부 지출(Transactions.Category) : 실제 2,937,790
   ├── 장부 수입(Transactions.SubCategory) : 회비·찬조 697,000
   ├── 연간계획(AnnualPlan.Title)    : 날짜·상태
   └── 사후보고서                   : 계획 vs 실제 + 정성 기록
```

**수입은 다르게 처리**: Category(회비수입/찬조금)를 유지해 회계 총계를 보존하고,
**SubCategory에 행사명**을 넣어 연결한다. (지출은 Category 자체가 행사명)

---

## 3. 확정된 설계 결정

| 항목 | 결정 |
|---|---|
| 결재 상태(승인/반려) | **불필요.** 만들지 않음 |
| 행사 입력 정보 | 제목·예상 날짜·예상 금액이면 충분 |
| 주간회의록 성격 | 준비 과정 협의의 **리마인드 용도** (행사 연결) |
| 사후 보고서 위치 | **별도 보고서 화면**에서 목록 관리 |
| 보고서의 실제 지출 | **장부에서 자동 집계** (Category 매칭) |
| 보고서 상세도 | 날짜·장소·인원·금액 + **잘된점·개선점·사진** |
| 연간계획 행사명 | 예산 카테고리 **datalist 자동완성** + 직접입력 병행 |
| 주차 계산 | 일요일 시작. 월주차 표시 + 연중주차 저장 |
| 분기/회계연도 | 부서별 설정 연동(`AccountingService`의 async 메서드 재사용) |

---

## 4. 맹점 (Blind Spots) — 계속 챙길 것

1. **계획 대비 실제(편차)가 인수인계의 핵심.** 보고서에서 예산·지출·수입을 나란히.
2. **"누가"의 기록.** 담당/책임자 필드가 아직 없음. 보고서에 추가 검토.
3. **연도 간 비교(YoY).** "작년 여름성경학교는 이랬다"를 나란히 보는 기능 — 미구현.
4. **미완료·취소 행사도 기록으로.** 상태값에 '취소' 추가 검토.
5. **월별장부 입력 UI에 SubCategory 없음.** 재분류는 했지만 앞으로 입력할 때
   행사·비용 구분이 안 들어감 → **우선순위 높음**.

---

## 5. 현재 구현 상태 (2026-08-02 기준)

| 기능 | 라우트 | 상태 |
|---|---|---|
| 연간 계획 | `/annual-plan` | ✅ 운영 배포됨 |
| 주간 회의록 | `/weekly-meeting` | ✅ 운영 배포됨 |
| 행사 사후보고서 | (미정) | ⬜ **다음 작업** |

**연간 계획 기능**: 카드 그리드, 분기+상태 필터, 날짜→회계연도·분기·주차 자동계산,
행사기간(시작~종료), 엑셀/CSV 일괄등록, 행사명 datalist 자동완성

**주간 회의록 기능**: 회의 날짜별 그룹, 행사 연결/일반안건 구분, 행사 필터,
행사 선택 시 일시·장소·예상인원 입력

### 엔티티 현황
- `AnnualPlan`: Id, DepartmentId, EventDate, EndDate, FiscalYear, WeekNo, Quarter,
  Title(행사명), Description, PlannedIncome, PlannedExpense, Status(PlanStatus)
- `WeeklyMeeting`: Id, DepartmentId, MeetingDate, FreeMemo, EventDateTime,
  EventLocation, ExpectedAttendees, AnnualPlanId
- `LedgerEntry`(Transactions): …, Category(=행사), **SubCategory**(수입:행사 / 지출:비용), …
- `BudgetPlan`: Year, DepartmentId, Type, **Category(=행사)**, SubCategory(=비용), CalcDetail, Amount

**⚠️ 이름 혼동 주의**: `ExpenseReport`는 기존 **결재/입금용 지출보고서**(Status, PaymentMethod).
우리가 만들 **행사 사후보고서와는 다른 것**. 별도 엔티티로 만들거나 용도를 명확히 구분할 것.

### 배포 상태
- **자동 배포 꺼짐** (`deploy.yml` → `workflow_dispatch`). Actions에서 수동 실행.
- 운영 서버 마이그레이션 **11건 적용 완료** (레거시 6 + 신규 5)
- `DbInitializer.Initialize()`의 `Database.Migrate()`가 앱 기동 시 자동 적용
  → **별도 `dotnet ef database update` 불필요**
- 유년부 2026 재분류 **운영 적용 완료** (별도 문서 참조)

---

## 6. 로드맵

### ✅ 완료
1. 연간계획 화면 + 주간회의록 화면
2. 연간계획 행사명 ↔ 예산 카테고리 정합성 (datalist)
3. 예산·장부 분류 체계 재정비 (유년부 2026)
4. Transactions에 SubCategory 추가 + 수입 행사 연결

### ⬜ 다음
5. **행사 사후보고서 화면** ← 현재 지점
   - 별도 화면 + 목록 관리
   - 행사 선택 → 장부에서 지출·수입 **자동 집계**
   - 예산 대비 실제 비교
   - 정성 기록: 잘된점·개선점·담당자·사진
6. 월별장부 입력 UI에 SubCategory 입력란 추가
7. 다른 부서(영유아부 등) 분류 재정비
8. 미뤄둔 UI 다듬기 (연간계획 카드에 목표수입 표시 등)

---

## 7. 작업 안전 수칙 (그동안 겪은 함정)

### Codespace 협업
- 대형 파일(`AccountingService.cs`)은 **덮어쓰지 말고 블록만 추가**
- 신규 razor는 드래그로 직접 배치, 엔티티/서비스는 프롬프트로 "추가"만
- 마이그레이션 생성 후 `Up`이 **AddColumn만** 하는지 확인. DropTable 있으면 중단
- 커밋 시 `church.db*`·`publish_output/` 제외. 푸시는 명시 지시 후에만

### Blazor/Razor 함정
- `@표현식` 뒤 한글은 `@(...)`로 감싼다 (한글도 C# 식별자라 파싱 오류)
- async는 `_ = ...`(fire-and-forget) **금지**, `await` 사용 (컴포넌트 크래시 원인)
- `@bind:after` 사용 시 `StateHasChanged()` 중복 호출 금지
- **모달 배경에 `@onclick="닫기"` 금지** — 입력란 클릭 시 이벤트 전파로 모달이 닫힘

### DB 작업
- 운영 DB 수정 전 **반드시 백업** (`cp church.db church.db.before_xxx_$(date …)`)
- SQL 실행 시 앱 중지 (`docker-compose stop`) → 실행 → 재시작
- **트랜잭션 스크립트는 `COMMIT;` 필수**, 실행 후 **별도 세션으로 재조회 검증**
  (자세한 내용은 `예산_분류체계_개선안.md` §6 참조)
- 배포는 무중단(`docker-compose down` 없이 build → up -d), SQL 실행만 중지 필요
